<!-- rigorous-theory-search-metadata {"contract_id":"contract-sha256-ebf8914b08524414858dcfd879ec3b08e5abd21bb0c9f8f36feb64d97f1cd7f2","schema_version":"rigorous-theory-search/v1","target_digest":"ebf8914b08524414858dcfd879ec3b08e5abd21bb0c9f8f36feb64d97f1cd7f2"} -->
# Independent reconstruction

## Method

A labeled sequential reconstruction pass read the frozen contract and
dependency DAG without the construction theorem, final report, executable, or
proof narrative. It rebuilt each target ancestor from the displayed
definitions and then compared the results. This separation is procedural, not
independent-agent corroboration.

## Reconstruction

For each atom,

\[
\frac{D}{P_\theta(x)}=P_\theta(\bar x),
\]

so strict positivity follows from \(|\kappa|<1\), and parity balance gives
normalization. Every proper marginal sums over an omitted bit; pairing its two
values cancels the perturbation. Singleton extraction is therefore a left
inverse of the polynomial lift, and differentiating the identity gives rank
six. Positivity of the categorical Fisher form then gives zero radical.

Simultaneous coordinate complementation preserves \(D\) and changes parity by
\((-1)^{|J|}\). Even complements preserve fixed \(\kappa\); odd complements
reverse it. This reconstructs the scalar/pseudoscalar boundary without a
preferred symmetry narrative.

Finite KL disintegration over a two-bit block and four-bit outside state
produces one outside-marginal KL plus an outside average of conditional KLs.
The common outside term cancels between two recognition laws. Differentiating
the finite conditional sum along normalized tangents gives the exact local
and global first-variation identity.

For an arbitrary joint score \(J=s+R\), bilinearity yields all four terms in
the Fisher expansion. In the parity family, all two-bit marginals are product,
and differentiating singleton expectations gives
\(\mathbb E[s_iJ_j]=\delta_{ij}/[\theta_i(1-\theta_i)]\). Hence both cross
matrices vanish and the joint tensor is the product metric plus the residual
Gram matrix. The explicit residual factor is nonzero on every atom for
\(\kappa\ne0\). A null linear combination reduces to a sum of six one-bit
functions; one-bit differences force every coefficient to zero. Thus the
residual is positive definite.

At the center, parity balance and cancellation over four unused bits give
\(4I_6/(1-c^2)\), and subtraction gives
\(4c^2I_6/(1-c^2)\). Comparing with a weighted diagonal metric produces the
exact additional diagonal defect and the center condition
\(w_i=1/(1-c^2)\).

For the hyperedge kernel, summing the parity likelihood under the uniform
prior gives evidence \(1/2\); division gives the target posterior. Posterior
recognition has VFE \(\log2\), while uniform recognition adds the exact KL
\(-\tfrac12\log(1-c^2)\). The linear four-atom record coupling separately
reconstructs the same two record marginals as the product coupling while
deleting its \(ab\) coefficient.

## Comparison and result

The reconstruction matches every dependency claim:
positive-normalized-lift, all-proper-marginals, symmetry-boundary,
right-inverse-immersion, fixed-outside-vfe, joint-fisher-decomposition,
parity-residual-positive, center-fisher, weighted-marginal-gate,
hyperedge-record, lift-dependence-boundaries, and target.

Result: PASS. The comparison also recovered the 57-dimensional
fixed-singleton fiber, the one-dimensional parity direction, the two-bit
indefinite correction, and the redundant-amplitude rank boundary. It derived
no canonical, intervention, agency, gauge, continuum, physical, or
renormalization conclusion.
