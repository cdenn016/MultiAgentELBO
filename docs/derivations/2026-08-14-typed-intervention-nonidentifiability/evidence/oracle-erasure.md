<!-- rigorous-theory-search-metadata {"contract_id":"contract-sha256-efe715cba81c2000403811015e6cecb599f4023133543b5cb0ae77288bccc874","schema_version":"rigorous-theory-search/v1","target_digest":"efe715cba81c2000403811015e6cecb599f4023133543b5cb0ae77288bccc874"} -->
# Semantic oracle-erasure audit

## Audit target

The audit covers the frozen target and every transitive dependency used for
its negative closure:

- `TARGET-PASSIVE-DETERMINES-REDUCED-INTERVENTIONS`;
- `CLM-PASSIVE-EQUALITY-L1-L2`;
- `CLM-REDUCED-NONISOMORPHISM`;
- `CLM-RESPONSE-IMAGE-OBSTRUCTION`;
- `CLM-FIFTEEN-BEHAVIORAL-CLASSES`;
- `CLM-REDUCTION-FUNCTOR`;
- `CLM-TWO-SIDED-CONGRUENCE`;
- `CLM-RIGHT-OVERRIDE-MONOID`; and
- `CLM-RAW-TYPED-PRESENTATIONS`.

The contract records no affirmative search prior. The semantic audit
nevertheless repeats the stronger operation: remove any expectation of a
negative result, inspect premises and proof steps for a paraphrase of the
conclusion, and recompute the entire dependency closure from definitions.

## Premise audit

The premises assert only the intervention rule, what the passive functor
retains, admission of two explicit BSC parameter pairs, and comparison by
complete joint tables. None asserts unequal active responses,
nonisomorphism, nonessential injectivity, or failure of recovery.

The modeling postulate merely declares a finite mathematical model class and
explicitly declines ontological canonicity. The equivalence relation defines
the two-sided response quotient but does not assert how many classes either
witness has. The choice of `L_1` and `L_2` is a proposed counterexample
witness permitted by the universal target; membership in the domain does not
encode the response-image mismatch.

## Closure recomputation without directional expectation

1. Finite DAG normalization follows by summing out sinks. Point-mass
   replacement preserves normalization, and typed bijections transport the
   product law. This closes `CLM-RAW-TYPED-PRESENTATIONS`.
2. Nodewise last-assignment selection is associative with the empty context
   as identity. This closes `CLM-RIGHT-OVERRIDE-MONOID`.
3. Equality of all two-sided contextual responses is an equivalence, and
   associativity proves stability under multiplication on both sides. This
   closes `CLM-TWO-SIDED-CONGRUENCE`.
4. Quotient multiplication, the descended response, and transport on raw
   isomorphisms are well-defined. This closes `CLM-REDUCTION-FUNCTOR`.
5. The BSC composition identity gives crossover `5/12` for both parameter
   pairs, hence the same passive joint table. This closes
   `CLM-PASSIVE-EQUALITY-L1-L2`.
6. Direct response calculation partitions the twenty-seven contexts into
   nine singletons and six size-three classes. This closes
   `CLM-FIFTEEN-BEHAVIORAL-CLASSES`.
7. The `L_1` law `(1/3,1/6,1/3,1/6)` has a two-element orbit under all
   four boundary flips, while the complete `L_2` response image contains
   neither orbit element. This closes
   `CLM-RESPONSE-IMAGE-OBSTRUCTION`.
8. Any reduced isomorphism would have to intertwine that missing response,
   so none exists. This closes `CLM-REDUCED-NONISOMORPHISM`.
9. Literal passive equality plus reduced nonisomorphism supplies an admitted
   counterexample to the universal implication, so the target is
   `REFUTED`.

Every step remains valid after removing any desired outcome. No step cites
the release status, a future ontology, the diagnostic total-variation
contrast, null-node inventory, agent agreement, or executable enumeration as
a mathematical premise.

## Paraphrase scan

The contract, claim statements, dependency edges, construction, strongest
result, and load-bearing proof steps were read for assumptions equivalent to
the conclusion. No premise says or implies that passive equality fails to
identify the reduced experiment. The only negative statements prior to the
conclusion are independently derived inequalities between exact rational
response atoms. The response mismatch is not built into the category
definition: other parameter pairs can have matching reduced responses.

The fixed BSC parameters do not create circularity. A counterexample to a
universal statement may select any two admitted objects, and their passive
equality and active mismatch are both proved after selection. Similarly, the
definition of reduced isomorphism does not name the witness or require
different response images.

## Non-load-bearing material

The following results survive as controls or consequences but are not in the
target's dependency ancestry:

- deletion of an independent null node after behavioral reduction;
- the direct/split/null shared-boundary tables;
- the diagnostic total-variation values `1/3` and `1/2`;
- nonessential injectivity and the downstream no-two-sided-recovery
  corollary;
- the logical allowance for a conventional representative-selecting section.

Removing all of them leaves the negative certificate intact.

## Result

`PASS`. The frozen target remains refuted, and every dependency ancestor
remains directly derived, after semantic erasure of any directional
expectation. This result establishes absence of prior dependence in the
recorded closure; it does not by itself prove the mathematical claims, whose
evidence remains the direct derivations and counterexample.

## Scope preserved

The audit does not convert open statements into theorems. Category
canonicity, autonomous agency, minimal realization, soft or continuous
interventions, continuum or gauge extensions, VFE emergence, and physical
identifications remain outside the certified closure.
