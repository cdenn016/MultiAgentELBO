<!-- rigorous-theory-search-metadata {"contract_id":"contract-sha256-efe715cba81c2000403811015e6cecb599f4023133543b5cb0ae77288bccc874","schema_version":"rigorous-theory-search/v1","target_digest":"efe715cba81c2000403811015e6cecb599f4023133543b5cb0ae77288bccc874"} -->
# Construction or strongest theorem

## Strongest directly derived result

Let `FinTIP_(R,O)^iso` be the frozen groupoid of finite normalized typed DAG
presentations with all partial hard assignments. Let `Red(P)` quotient the
right-override context monoid by equality of the complete two-sided retained
response signature, and let

```text
Red : FinTIP_(R,O)^iso -> FinRIE_(R,O)^iso,
Ubar_pass : FinRIE_(R,O)^iso -> FinObs_(R,O)^iso.
```

The direct derivations in `evidence/typed-category-proof.md` and
`evidence/operational-reduction-proof.md` establish that these objects and
functors are well-defined in the declared finite category.

**Finite typed-intervention nonidentifiability theorem.** In the declared BSC
subcategory, equality of the complete passive retained `(R,O)` law does not
imply isomorphism of reduced protocol-indexed retained response experiments.
Specifically,

```text
L_1 = L(a=1/4,b=1/3),
L_2 = L(a=1/3,b=1/4)
```

share the passive law `(7/24,5/24,5/24,7/24)` and the same raw structural
signature. Each operational quotient has exactly fifteen behavioral classes.
The `L_1` response

```text
(1/3,1/6,1/3,1/6)
```

from `do(E=0)` is absent from the complete `L_2` reduced response image
under every one of the four admitted typed binary boundary relabelings.
Therefore `Red(L_1)` and `Red(L_2)` are nonisomorphic. This exact
response-image mismatch, not unequal mediator total variation and not
null-node count, is the negative certificate for the frozen universal
implication.

**Recovery corollary.** `Ubar_pass` is not essentially injective on this
subcategory. Consequently no functor `R` can satisfy

```text
R Ubar_pass ~= identity_(FinRIE_(R,O)^iso)
```

there. This does not exclude the conventional representative-selection
condition `Ubar_pass R ~= identity_(FinObs_(R,O)^iso)`.

## Stronger operational control

Adding an independent isolated binary node `N` to the split presentation
does not change any retained response after any assignment to `N` and any
of the twenty-seven partial contexts on `{R,E,O}`. Forgetting `N`
induces an identity- and response-preserving quotient-monoid isomorphism.
Thus the negative theorem survives removal of inert node padding.

## Certification and scope

The theorem has direct derivation evidence, and the exact witness provides
corroborative finite controls. Independent reconstruction reproduces the
definitions, normalization, quotient, fifteen-class table, passive equality,
and full four-relabeling response-image obstruction. The adversarial portfolio
rejects every attack on the frozen dependency closure, and semantic oracle
erasure leaves the closure unchanged. The associated release is therefore
`COMPLETE_NEGATIVE` for the frozen universal target.

No result here establishes category canonicity, autonomous agency, arbitrary
latent-dilation equivalence, soft or continuous intervention semantics,
continuum or gauge extensions, VFE emergence, or a physical ontology.
