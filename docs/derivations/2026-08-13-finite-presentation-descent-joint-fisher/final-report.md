<!-- rigorous-theory-search-metadata {"contract_id":"contract-sha256-c68f474d3e7991fa78a557b86bd645dc1c01a4272b2a75d53a52e17ad29176b2","schema_version":"rigorous-theory-search/v1","target_digest":"c68f474d3e7991fa78a557b86bd645dc1c01a4272b2a75d53a52e17ad29176b2"} -->
# Rigorous theory search report

## Frozen contract

Contract
`contract-sha256-c68f474d3e7991fa78a557b86bd645dc1c01a4272b2a75d53a52e17ad29176b2`
asks exactly which VFE and Fisher objects descend under finite observational
presentation equivalence and whether paired marginal sections determine the
full joint objects. The domain is finite normalized probability, with a
strictly positive regular categorical tier for Fisher claims. Autonomous
agency, continuum closure, and physicalization are excluded.

## Terminal status

`COMPLETE_AFFIRMATIVE` for the frozen mathematical target.

## Certificate

Let finite presentations have the same retained joint law \(P_{RO}\) and
completed retained conditioning algebra. Then:

1. Their collapsed retained-variable VFE is identical.
2. Every full-latent VFE equals the collapsed VFE plus an expected auxiliary
   conditional KL. On \(Q_R\ll P_R(\cdot\mid o)\), equality and conditional
   minimization occur exactly at posterior completion, with a unique
   minimizing joint law.
3. Every strictly positive \(C^2\) retained categorical family that agrees
   parameterwise has the same retained Fisher tensor and the same common
   \(C^1\) configuration pullback.
4. Two explicit smooth positive right-inverse lifts of the same paired binary
   marginals have different full-joint VFE values and different Fisher
   pullbacks. Marginals alone therefore do not determine those objects.

The full statement and proof dependencies are in
`construction-or-strongest-theorem.md`.

## Strongest verified result

The finite observational quotient supports a sharp invariance rule. Evidence,
retained posteriors, collapsed VFE landscapes and minima, retained divergences,
and parameterwise retained Fisher tensors factor through the quotient.
Posterior-completed or conditionally minimized full VFE also factors through
it. Arbitrary uncompleted full-latent VFE, auxiliary posteriors, full-joint
Fisher tensors, node counts, factorizations, labels, and typed intervention
structures do not generally factor.

The binary direct/latent/null-node witness proves this separation exactly. A
second independently reconstructed binary family reaches the same boundary.
The categorical lift witness proves that the dependence sector must be
declared even when no auxiliary environment presentation is involved.

## Dependency closure

All target ancestors are `EVIDENCE_VERIFIED` by direct derivations:

- collapsed VFE descent and conditional-KL completion;
- strict non-descent of arbitrary full-latent VFE;
- parameterwise retained Fisher descent;
- exact binary presentation separation;
- smooth right-inverse joint lifts;
- strict VFE and Fisher lift dependence; and
- paired-marginal noncanonicity.

The deterministic witness reproduces the exact rational matrices, gaps, and
eigenvalues but is corroboration, not the mathematical proof.

## Independent reconstruction

An isolated contract-only reconstruction derived every target dependency
before opening the run's proof artifacts. It also supplied a different
direct-versus-latent binary witness with the same retained/full separation.
Its conclusion is `PASS`; see `evidence/independent-reconstruction.md`.

## Oracle erasure

The oracle check removed the affirmative prior, all status fields, the desired
ontology, every preferred-lift premise, and the contract's semantically
redundant right-inverse premise. Every target dependency remained derivable
from finite normalization, disintegration, score calculus, and the explicit
formulas. Its conclusion is `PASS`; see `evidence/oracle-erasure.md`.

## Unresolved obligations

The target is closed, but these nondependencies remain open:

- a canonical agentization or autonomous-agency criterion;
- equivalence enriched enough to preserve declared interventions;
- selection of one admissible joint lift from paired sections;
- continuum probability and well-posed section-space dynamics;
- a shared physicalization rule, causal/signature structure, time, units, and
  dimensional constants.

## Scope and limitations

This is a finite mathematical theorem. It licenses an operational
environment-node presentation and identifies which retained informational
objects are presentation invariant. It does not establish that an auxiliary
node is an autonomous agent, that the observational quotient is ontological
identity, or that a descended Fisher tensor is physical geometry. It makes no
novelty or priority claim.
