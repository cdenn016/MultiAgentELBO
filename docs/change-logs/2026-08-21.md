# 2026-08-21 change log

## Local-first renormalization v2 planning

- Added the approved Release 1 design for a local-first renormalization and coarse-graining laboratory.
- Located the new implementation directly under repository-root `rg_v2/`; it remains repository-local and is excluded from installed `multiagent_elbo` package discovery.
- Defined exact finite agent, recognition, population, posterior, common-channel aggregate, fixture, metric, and six-artifact contracts.
- Added a phased implementation plan covering additive shared configuration and registry seams, exact local construction, recognition selection, posterior derivation, one-step coarse-graining, RunStore publication, v1 regression protection, and focused verification.
- Kept gauge covariance, multiscale composition, regeneration, legacy adapters, bounded generalization, rendering, and any promotion of an aggregate to an agent outside Release 1.

This was a documentation-only planning pass based on `origin/main` revision `c04a56e80abf3fd956941aa0021a3a93ea53eaae`. No source code was changed and no tests or experiments were run.

## Local-first renormalization v2 Release 1 implementation

Release 1 was implemented against scientific baseline `c04a56e80abf3fd956941aa0021a3a93ea53eaae`. The laboratory is directly under repository-root `rg_v2/`, depends one-way on public `multiagent_elbo` primitives, and remains excluded from installed-package discovery. `pyproject.toml`, package discovery, pytest metadata, and coverage metadata are unchanged from the baseline. The terminal semantic object is `AggregateDatum`; Release 1 does not promote an aggregate into an agent.

The implementation added `rg_v2/{contracts,population,coarse,fixtures,experiment}.py`, the minimal `rg_v2/__init__.py` and `rg_v2/README.md`, the product, declared-correlated, Dirac-boundary, and frozen v1 data files under `rg_v2/data/`, the root click-to-run launcher `run_renormalization_v2_lab.py`, additive configuration and registry seams in `src/multiagent_elbo/config.py` and `src/multiagent_elbo/experiment_support.py`, the focused `tests/rg_v2/` suite, and additive checks in `tests/test_config.py` and `tests/test_experiment_support.py`.

The three executable fixture IDs are `lf3_product_v1`, `lf3_correlated_v1`, and `lf3_dirac_boundary_v1`. Each finalized run publishes exactly six semantic artifacts: `fixture_snapshot.json`, `population_joint.json`, `population_inference.json`, `aggregate_datum.json`, `metrics.json`, and `arrays.npz`. The fixed thirteen-metric inventory is `agent_kernel_normalization_residual`, `evaluator_compatibility_residual`, `record_kernel_normalization_residual`, `record_ownership_violation_count`, `population_normalization_residual`, `independent_population_residual`, `recognition_marginal_residual`, `model_marginal_non_dirac_count`, `posterior_derivation_residual`, `common_channel_identity_violation_count`, `coarse_evidence_residual`, `conditional_kl_defect`, and `kl_chain_residual`. Every new metric remains `CANDIDATE`.

Artifact-only replay was exercised for all three fixtures with the primitive loader disabled and `rg_v2/data` reads guarded. Replay rebuilt exact agents, records, local recognitions and derived marginals, selectors including the declared correlated coupling, both population constructions, inference, the common channel, and `AggregateDatum`; it independently recomputed the channel hash, aggregate route identity, twelve mathematical or structural metric records, and the scoped identity record. The NPZ gate required exactly twenty members, Unicode identity/provenance arrays, fourteen float64 mirrors with fixture-derived shapes, and `allow_pickle=False`.

The launcher ran by absolute path from an arbitrary working directory with an empty inherited `PYTHONPATH`. Its AST contains one `sys.path.insert` whose value is `SRC`, and installed-package sources have no reverse import of `rg_v2`. An offline temporary-copy wheel build through `setuptools.build_meta.build_wheel` contained `multiagent_elbo` and excluded top-level `rg_v2`; an isolated extracted-wheel subprocess imported `multiagent_elbo` and found no `rg_v2` specification.

The v1 regression created a fresh detached worktree at `c04a56e80abf3fd956941aa0021a3a93ea53eaae` in an operating-system temporary directory outside the repository, captured it with `C:\Python314\python.exe -I -B` and an empty inherited `PYTHONPATH`, removed the detached worktree, then captured the current revision through the same isolated protocol. Both sides matched the frozen twelve launcher configuration JSON/SHA identities, source fixture hash, seven scale-cocycle semantic hashes, fourteen complete `MetricRecord` dictionaries, and protected installed-package blobs excluding only the additive configuration and registry seams. Normal regression did not write the frozen manifest.

The final focused CPU command was:

```powershell
C:\Python314\python.exe -B -m pytest tests\rg_v2 tests\test_config.py tests\test_experiment_support.py -q -p no:cacheprovider --basetemp=.pytest-rg-v2-release1 --junitxml=.pytest-rg-v2-release1.xml
```

Its JUnit file recorded 264 tests: 262 passed, 2 skipped, 0 failures, and 0 errors in 21.882 seconds. The required broader CPU command was:

```powershell
C:\Python314\python.exe -B -m pytest tests -q -p no:cacheprovider --basetemp=.pytest-rg-v2-broad --junitxml=.pytest-rg-v2-broad.xml
```

Its JUnit file recorded 1,652 tests: 1,634 passed, 18 skipped, 0 failures, and 0 errors in 366.885 seconds. Both commands ran with `CUDA_VISIBLE_DEVICES=-1` and `PYTHONHASHSEED=0`. This release makes no CUDA claim.

## Local-first renormalization v2 release-gate hardening

The artifact-only replay gate now parses `metrics.json` fail-closed before constructing its name-to-record mapping: the raw artifact must contain exactly thirteen records, every name must be unique, and the ordered name tuple must equal the registry inventory. Two mutation cases first demonstrated that a duplicate name and an appended extra record reached later comparisons; both are now rejected at the raw-record boundary. The isolated extracted-wheel check now imports `multiagent_elbo`, resolves its `__file__`, requires that file to live under the extracted wheel root, and still requires `find_spec("rg_v2")` to return `None`.

The final focused CPU JUnit recorded 266 tests: 264 passed, 2 skipped, 0 failures, and 0 errors in 23.913 seconds; its XML modification time was `2026-08-21T21:17:31.275658Z`. The first broader attempt recorded 1,654 tests: 1,635 passed, 18 skipped, 1 failed, and 0 errors in 385.026 seconds. The sole miss was the unrelated tight floating-point retention assertion in `test_the_published_retentions_are_unchanged_by_the_remediation`. It passed alone, with its complete module, after the complete RG-v2 suite, after every legacy predecessor in the broad collection order, and under one- and twenty-four-thread OpenBLAS settings. No unrelated source or tolerance was changed. The one authorized unchanged broader rerun then recorded 1,654 tests: 1,636 passed, 18 skipped, 0 failures, and 0 errors in 400.529 seconds; its XML modification time was `2026-08-21T21:50:30.099142Z`.

Both final commands used `CUDA_VISIBLE_DEVICES=-1` and `PYTHONHASHSEED=0`. This hardening makes no CUDA claim and does not change production science, experiment, configuration, registry, packaging metadata, the thirteen `CANDIDATE` metrics, or the `AggregateDatum` endpoint.

## RG-v2 recursive coarse-agent closure design

Added the approved Phase 2 design for a static two-parent recursive closure witness. The design starts from four fine agents in two declared blocks, constructs two ordinary next-level agent mechanisms and one exact combined record from the pushed generative joint, declares a bijection from the fine observation support to the compound-record support, supplies identity access on that reconstructed support and an all-observation Bayesian update table, and requires the unchanged Release 1 population and inference constructors to reproduce coarse recognition, evidence, and posterior for all sixteen positive-evidence observations.

The specification keeps generative construction independent of access, update, recognition, selector, realized observation, posterior, `PopulationInference`, and `AggregateDatum`; preserves `AggregateDatum` as an unmodified terminal Release 1 type; requires at least one exact violation of a fully declared sparse-record factorization alongside the dense exact fallback; and fixes the root-local module, config, artifact, metric, replay, regression, and countercontrol boundaries for implementation planning. The construction proves static closure only in the Release 1 combined-state `AgentDatum` family; Chapter 4's separated belief/model factorization, observation-dependent recognition-family descent, autonomous dynamics, lumpability, scale composition, fixed points, universality, and continuum claims remain open.

Three independent design reviews checked the live code surface, the mathematical obligations, and the experiment contract. Their blocking findings were incorporated before finalization: the observation supports are related by an explicit typed bijection; access and Bayesian update data have an explicit inference-free constructor and live descent control; update data reside in the interface artifact rather than the generative artifact; sparse factorization is mechanically identifiable; and the executable non-lumpable dynamics control was removed from this static phase.

This was a documentation-only design pass based on `origin/main` revision `d4ce80bc9eda90ec54dc9c97d8405d7d28ca6182`. No source code was changed and no tests or experiments were run.

## RG-v2 recursive coarse-agent closure implementation plan

Added the seven-task implementation plan for the approved Phase 2 specification. The tasks cover frozen recursive contracts, the strict `lf4_two_parent_recursive_v1` primitive fixture, exact coarse generative reconstruction, separated information/update and recognition paths, additive config and registry wiring, eight-artifact publication, artifact-only replay, frozen Release 1 regressions, and final focused and broader CPU gates.

A read-only exact-rational fixture probe confirmed that all sixteen coarse latent states and all sixteen observations have positive mass. The declared sparse family has 448 failed exact identities under the fixed counting convention and maximum conditional total variation `47889/245000`; the dense combined-record reconstruction remains the admitted exact path. Each parent has six distinct update rows. The even-total-belief-parity selector pushes to eight coarse states of mass `1/8`, gives both parent model marginals `(1/2,1/2)`, and is genuinely non-product.

This planning pass changed documentation only. No production code or test source was changed, and no repository test suite or experiment was run.

## RG-v2 recursive coarse-agent closure release gates

Task 7 closed the Phase 2 release gates from clean revision b04dc0138a6ad1815480273d417c58a50d4dbf36, whose recursive scientific baseline is d4ce80bc9eda90ec54dc9c97d8405d7d28ca6182. The only tracked Task 7 changes are the recursive experiment test and this dated log. Release 1 frozen values, production science, configuration, registry, launcher, README, packaging metadata, and the unchanged legacy regression controller were not edited.

Artifact-only replay accepts only a finalized run directory and operates with primitive fixture loading and rg_v2/data reads disabled. It reconstructs the four fine agents, four records, recognition family, selector, fine population and inference, recursive structure, two coarse parent mechanisms, compound record, access and update tables, coarse recognition, coarse population and inference, all sixteen observation round trips, and both terminal aggregates. Independent exact-rational oracles cover all 4,096 fine cells and all 256 coarse cells, enforce the channel/hash/bijection identities, and reproduce the declared sparse countercontrol at 448 failed identities with maximum conditional total variation 47889/245000.

Replay rederives the four semantic fixture subhashes from the embedded typed snapshot. The raw fixture SHA-256 is provenance-only because the finalized artifacts do not contain the original raw fixture bytes; the gate therefore validates its canonical digest form and all downstream envelope and dependency consistency without reading the forbidden source. The exact artifact DAG covers fixture_snapshot.json, fine_population.json, coarse_generative.json, coarse_interfaces.json, coarse_population.json, all_observation_inference.json, metrics.json, and arrays.npz. Metrics are rejected before mapping unless the raw list contains exactly twenty unique, well-formed records in registry order. Every NPZ is opened with allow_pickle=False and checked for exact members, dtypes, shapes, logical hash, and JSON mirrors. Mutation gates reject duplicate, extra, reordered, and malformed metrics; invalid semantic hashes and rational denominators; altered observation pairs; and object-dtype arrays.

The launcher ran once by absolute path from an arbitrary working directory with an empty inherited PYTHONPATH. Its AST has exactly one sys.path insertion. The source tree has no reverse import from the installed package into rg_v2. An offline wheel built from a temporary repository copy imported the extracted multiagent_elbo package from the extracted wheel root and excluded top-level rg_v2.

The complete Task 7 targeted selection recorded 13 passed and 21 deselected in 46.19 seconds. The unchanged legacy regression controller recorded 6 passed and 1 skipped in 7.31 seconds using an operating-system temporary directory outside the worktree; frozen values were not refreshed.

The final focused CPU command set CUDA_VISIBLE_DEVICES=-1 and PYTHONHASHSEED=0 in the same PowerShell process as pytest, then ran C:\Python314\python.exe -B -m pytest tests\rg_v2 tests\test_config.py tests\test_experiment_support.py -q -p no:cacheprovider --basetemp=.pytest-rg-v2-phase2 --junitxml=.pytest-rg-v2-phase2.xml. Its mechanically parsed JUnit recorded 381 tests: 379 passed, 2 skipped, 0 failures, and 0 errors in 107.892 seconds; the XML modification time was 2026-08-22T03:51:39.2165228Z.

The first broader invocation ran from a fresh shell without the required environment assignments and was preserved byte-for-byte as .pytest-rg-v2-phase2-broad-failed-env.xml. It mechanically recorded 1,769 tests: 1,732 passed, 18 skipped, 19 failed, and 0 errors in 387.806 seconds, with SHA-256 122025E1FD52F337914B0C97AC9122C9D9C784A1770E53BAFF92C06AB19BBFE2. Eighteen failures were the remediation environment sentinel; the remaining failure was the previously isolated process-state-sensitive retention assertion, which has no Phase 2 dependency. No source, test, or tolerance was changed in response.

The one authorized corrected broader gate set CUDA_VISIBLE_DEVICES=-1 and PYTHONHASHSEED=0 in the same PowerShell process and ran the unchanged broad command. Its mechanically parsed JUnit recorded 1,769 tests: 1,751 passed, 18 skipped, 0 failures, and 0 errors in 451.859 seconds; the XML modification time was 2026-08-22T04:08:14.3727566Z and its SHA-256 was 0E99616E9F0B9B462CAC2139742423C9B291D4171A1368BC347A95E380F21D9C.

All release gates used C:\Python314\python.exe as the CPU interpreter. This Task 7 evidence makes no CUDA claim.

## RG-v2 recursive coarse-agent closure review-fix iteration 1

The review-fix replay now constructs an independent complete serialized mapping for every one of the twenty ordered MetricRecords. It requires the exact eight-key record shape; built-in float values and tolerances, excluding booleans; built-in string metadata; and exact value, tolerance, status, assessment scope, theorem status, verification state, claim origin, and interpretation. Coherently rehashed mutations of theorem status, claim origin, interpretation, boolean value, and boolean tolerance are rejected at the named MetricRecord boundary.

The replay also requires the exact manifest, provenance, and input-hash key sets. It validates the ordered semantic_artifact_sha256 mapping, ordered arrays_direct_inputs names, ordered input_hashes.fixture_direct_inputs records, and ordered input_hashes.semantic_artifacts mapping against independently recomputed artifact and logical-array data. Isolated corruption, reordering, and extra-key mutations fail at their named manifest boundaries. The eight-artifact inventory is correctly named fixture_snapshot.json, fine_population.json, coarse_generative.json, coarse_interfaces.json, coarse_population.json, all_observation_inference.json, metrics.json, and arrays.npz.

The required review RED recorded 15 failed and 34 deselected in 108.40 seconds. After replay hardening, the exact mutation selection recorded 15 passed and 34 deselected in 94.16 seconds, and the complete recursive experiment selection recorded 49 passed in 175.21 seconds.

The first review broad gate mechanically recorded 1,784 tests: 1,764 passed, 18 skipped, 2 failed, and 0 errors in 545.871 seconds. The two failures were pre-existing numerical boundaries outside the Phase 2 replay change: a zero mutual-information ceiling bypassed a guard written only for positive roundoff residue, and a process-sensitive retention value exceeded a tight frozen decimal tolerance. Its XML is preserved as .pytest-rg-v2-phase2-broad-review-failed.xml with SHA-256 32EF72C36FB93B2C08B5A85D503C43504A413B6835874C1DB1F090A69B034032.

Commit 1e42721 attempted to reject the exact-zero ceiling, but the unchanged frozen-v1 controller correctly rejected its cocycle_flow.py blob. That focused gate mechanically recorded 396 tests: 393 passed, 2 skipped, 1 failed, and 0 errors in 203.810 seconds. Its XML is preserved as .pytest-rg-v2-phase2-review-production-failed.xml with SHA-256 B1123F5FFDA530EDDF6A1BF0E5EB3795A1D499E8D5AE927230694FD2DC93FB57. Commit d12f179 reverted that unapproved production change. The restored broad gate again exposed the same two numerical boundaries and recorded 1,784 tests: 1,764 passed, 18 skipped, 2 failed, and 0 errors in 552.165 seconds; its preserved XML SHA-256 is 151BC526CED55ADACD52AAC8861E134DDDB3D715221C4776F0BDF94295094233.

The separately authorized amendment 244f489 made information-retention ceilings fail closed and added its production regression coverage. Amendment 665743f then re-pinned the frozen v1 identity after that authorized remediation. These changes are separate from the Task 7 replay hardening.

At exact revision 665743f20421eab4e82b151318a628c53805089a, the final focused JUnit mechanically recorded 396 tests: 394 passed, 2 skipped, 0 failures, and 0 errors in 203.679 seconds. Its XML modification time was 2026-08-22T13:20:43.7011361Z and its SHA-256 was 5E2B4933C60DAE117CAEADA30F5C1F3EE7102970D359F5CB87B94EEE2FA41400.

The final broad JUnit mechanically recorded 1,788 tests: 1,770 passed, 18 skipped, 0 failures, and 0 errors in 549.268 seconds. Its XML modification time was 2026-08-22T13:30:17.0072137Z and its SHA-256 was D27EE97969AAB9DE4EA897559D790560FB2F22A23EA706A0B2CF7065A1BE5244. Both final commands set CUDA_VISIBLE_DEVICES=-1 and PYTHONHASHSEED=0 in the same PowerShell process and used C:\Python314\python.exe. This review fix makes no CUDA claim.

## RG-v2 recursive coarse-agent closure review-fix iteration 2

The second review fix closes the remaining artifact-only replay gaps without changing production code. Manifest replay now type-checks and binds provenance config_hash, fixture_id, arithmetic, effective_backend, and effective_dtype to the independently reconstructed configuration and artifact envelopes. It also requires input_hashes.resolved_config_sha256 to equal the reconstructed configuration hash and input_hashes.theory_tree_sha256 to equal the provenance theory SHA-256. Isolated valid-digest, wrong-value, and wrong-built-in-type mutations exercise these previously unbound fields.

Final-artifact replay now requires the exact fine_population payload key set and independently reconstructs fine_input_hashes. It requires the exact coarse_generative payload key set and reconstructs the serialized observation_bijection and sparse_record_candidate copies from the typed recursive structure. It also requires the exact coarse_population payload key set and reconstructs the complete runtime_oracle_pushed_joint mapping, including context, latent and observation supports, canonical rational masses, combined-channel digest, and exact keys. Downstream envelope inputs, semantic hashes, NPZ direct-input hashes, and both manifest hash mirrors are coherently rehashed in every mutation case, so each rejection occurs at its named serialized boundary.

Every Unicode provenance member in arrays.npz is now checked against its exact canonical NumPy dtype.str, with the expected width independently derived from the reconstructed scalar or vector strings. A value-preserving, coherently rehashed schema_version width expansion proves that Unicode kind and value checks alone are insufficient. The required RED recorded 21 failed and 49 deselected in 151.29 seconds; every failure was the expected DID NOT RAISE result against the prior replay. The single targeted GREEN recorded 21 passed and 49 deselected in 85.67 seconds.

The test-only gate revision is b4a82d410c902820284a718fe023426b6b0cbbdb, committed as `fix: bind recursive replay provenance`. The exact focused command set CUDA_VISIBLE_DEVICES=-1 and PYTHONHASHSEED=0 in the same PowerShell process and ran C:\Python314\python.exe -B -m pytest tests\rg_v2 tests\test_config.py tests\test_experiment_support.py -q -p no:cacheprovider --basetemp=.pytest-rg-v2-phase2 --junitxml=.pytest-rg-v2-phase2.xml. Its mechanically parsed JUnit recorded 417 tests: 415 passed, 2 skipped, 0 failures, and 0 errors in 284.831 seconds. The XML contains 65,672 bytes, has modification time 2026-08-22T14:06:32.1019126Z, and SHA-256 9F954EB6CC656D6111750342C178EECFE9664FDB9D0891119A09F9EE14C0B64D.

At the same clean test revision, the exact broad command used the same environment assignments and ran C:\Python314\python.exe -B -m pytest tests -q -p no:cacheprovider --basetemp=.pytest-rg-v2-phase2-broad --junitxml=.pytest-rg-v2-phase2-broad.xml exactly once. Its mechanically parsed JUnit recorded 1,809 tests: 1,791 passed, 18 skipped, 0 failures, and 0 errors in 625.873 seconds. The XML contains 278,168 bytes, has modification time 2026-08-22T14:17:50.6663850Z, and SHA-256 18282A39534F4C3D373D56A483AC8321A2431F86D664A6E92756EE738C8B3F6B.

The four historical failed XMLs remain preserved byte-for-byte alongside the two replacement success XMLs. The subsequent `docs: record final recursive verification` commit changes only this dated log; it is not the gate revision, and the test suites are intentionally not rerun after that docs-only commit. Both final gates used C:\Python314\python.exe as the CPU interpreter with CUDA_VISIBLE_DEVICES=-1. This review fix makes no CUDA claim.
