# Operational Intervention Extensions Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Establish and publish operational-quotient minimality plus rigorously scoped finite soft/randomized and compact-Feller continuous intervention extensions.

**Architecture:** A new rigorous-theory package separates the probability-free syntactic quotient, compact topological regularity, finite local-kernel and randomized-protocol constructions, and a smooth circle heat-kernel counterexample. Central theory receives only released claims, while raw latent/DAG realization canonicity remains open.

**Tech Stack:** Python 3.14 standard library (`fractions`, `decimal`, `json`, `hashlib`), LaTeX/pdflatex, rigorous-theory-search validator, verification schema 1.1, Git worktrees.

**Spec:** `docs/superpowers/specs/2026-08-14-operational-intervention-extensions-design.md`

## Global Constraints

- Work only in `C:\Users\chris and christine\Documents\ChatGPT\MultiAgentELBO-intervention-extensions` on `codex/intervention-extension-20260814`, based on `origin/main` at `53cafa374a668ea017a7c50ddbeb75e9045f73a8`.
- Use American English in every added or edited line.
- Do not modify any prior derivation package; the new package is additive.
- Do not run the repository pytest suite or any model/GPU workload. Use exact standalone recomputation, release validators, static checks, and TeX builds only.
- Mathematics closes only by direct derivation. Exact rational and decimal execution is corroborative.
- The universal quotient is canonical only over a fixed raw protocol monoid and quotient map. Never call it a canonical latent/DAG realization.
- Target/type-face preservation is explicit and load-bearing for local soft and continuous mediator results.
- The randomized-protocol theorem requires independent convolution and response-compatible affine unital convolution-monoid isomorphisms. It does not cover correlated sequential randomization.
- Standard-Borel semantics uses standard-Borel palettes, jointly measurable pointwise kernel evaluation, the evaluation sigma-algebra on retained laws, and finite topological-order Ionescu--Tulcea recursion. It does not infer intervention behavior from almost-sure observational conditionals.
- The compact-Feller tier uses `A=product_v({bottom_v} disjoint-union J_v)` with compact metrizable palettes, isolated bottom symbols, coordinatewise right override, and jointly continuous multiplication, plus jointly Feller kernels and a continuous response.
- The circle witness fixes the mediator parameter experiment and circle heat geometry; admitted relabelings must preserve that geometry and explicitly intertwine responses. It does not establish an uncolored arbitrary-kernel no-go.
- New derivation-package files use LF through an exact `.gitattributes` rule. Central theory/status files retain their existing CRLF convention.
- Any post-ledger tracked edit invalidates formal closure and requires a new artifact revision and ledger validation.

---

### Task 1: Commit the approved design and freeze the theorem target

**Files:**
- Create: `docs/superpowers/specs/2026-08-14-operational-intervention-extensions-design.md`
- Create: `docs/superpowers/plans/2026-08-14-operational-intervention-extensions.md`
- Modify: `.gitattributes`
- Create: `docs/derivations/2026-08-14-operational-intervention-extensions/problem-contract.json`

**Interfaces:**
- Consumes: released hard-intervention and canonical-selection packages at baseline `53cafa3`.
- Produces: committed design/plan plus one digest-bound target for all later evidence.

- [ ] **Step 1: Copy the approved design and this plan into the isolated worktree with their intended EOLs.**

Run `git ls-files --eol docs/superpowers/specs docs/superpowers/plans | Select-Object -Last 10` and preserve the existing CRLF/LF conventions.

- [ ] **Step 2: Add one scoped LF rule.**

Append exactly:

```gitattributes
docs/derivations/2026-08-14-operational-intervention-extensions/** text eol=lf
```

- [ ] **Step 3: Scaffold the rigorous-theory package.**

Run:

```powershell
& 'C:\Python314\python.exe' 'C:\Users\chris and christine\.agents\skills\rigorous-theory-search\scripts\scaffold_run.py' --date 2026-08-14 --slug operational-intervention-extensions 'docs\derivations'
```

- [ ] **Step 4: Freeze the conjunction target and its exclusions.**

The target must name the algebraic/coarsest quotient, compact topological quotient, finite target-stratified soft theorem, affine randomized-protocol theorem, compact-Feller standard-Borel tier, and heat-kernel nonidentifiability. It must explicitly exclude canonical raw realization, target-erasing arbitrary soft categories, correlated randomization, uncontrolled regular-conditional versions, noncompact quotient regularity, VFE/ELBO emergence, agency, gauge/RG extension, and ontology.

- [ ] **Step 5: Validate and commit the frozen input.**

Run checkpoint validation, JSON parsing, `git diff --check`, UTF-8/EOL/C0 scans, then commit:

```powershell
git commit -m "docs: design operational intervention extensions"
```

### Task 2: Write direct proofs and exact recomputation evidence

**Files:**
- Create: `docs/derivations/2026-08-14-operational-intervention-extensions/evidence/direct-derivation.md`
- Create: `docs/derivations/2026-08-14-operational-intervention-extensions/evidence/counterexample-proofs.md`
- Create: `docs/derivations/2026-08-14-operational-intervention-extensions/evidence/recompute.py`
- Create: `docs/derivations/2026-08-14-operational-intervention-extensions/evidence/recompute-output.json`
- Modify: package `construction-or-strongest-theorem.md`
- Modify: package `counterexample-register.md`

**Interfaces:**
- Consumes: Task 1 target digest and prior hard-intervention response tables.
- Produces: self-contained proofs and machine-readable corroboration used by release assembly.

- [ ] **Step 1: Prove the syntactic universal property.**

Show that contextual equivalence is the largest congruence in `ker(Phi)`, construct the unique finer-to-coarser factor map, prove finite minimum cardinality, and state uniqueness only over the raw quotient map. Include the four-element flag-monoid automorphism counterexample to bare uniqueness.

- [ ] **Step 2: Prove the compact topological theorem.**

Use a countable dense context set, the continuous signature map, compact-to-Hausdorff quotient topology, and the product quotient map to prove compact metrizability and continuous descended multiplication. State the terminal continuous universal property with exact quotient-map hypotheses.

- [ ] **Step 3: Prove finite normalized soft replacement and BSC target-face nonidentifiability.**

Derive normalization, right override, response continuity, the formula `P(O=0|R=r)=b+(1-2b)t_r`, and the exact TV diameter for `0<epsilon<1/2`. For every `epsilon<s_-<s_+<1-epsilon`, prove the interior separation `|1-2b|(s_+-s_-)`; do not use fixed `1/4,3/4` points uniformly in epsilon.

- [ ] **Step 4: Prove the affine randomized-protocol theorem.**

Define independent simplex convolution and affine response. Derive the contextual-signature minor and determinant formula, explain why full rank makes stochastic equivalence equality, and prove any response-compatible affine unital convolution-monoid isomorphism restricts to a forbidden hard vertex isomorphism. Include the exact convex-mixture counterexample showing why the old unmatched-response invariant no longer works, and scope full-simplex spanning to the four-atom witness whose four boundary point interventions are its four vertices.

- [ ] **Step 5: Prove standard-Borel and compact-Feller semantics.**

For the Borel tier, use standard-Borel palettes, joint evaluation measurability of the declared normalized kernels, the evaluation sigma-algebra on `P(X_ret)`, and finite topological-order Ionescu--Tulcea recursion. For the compact tier, use compact Polish node spaces and `A=product_v({bottom_v} disjoint-union J_v)` with compact metrizable palettes, isolated bottom symbols, coordinatewise right override, jointly continuous multiplication, and jointly Feller kernels; then prove weak response continuity and apply the compact quotient theorem. State why arbitrary all-kernel parameter spaces and an automatic standard-Borel or noncompact quotient are not claimed.

- [ ] **Step 6: Prove the circle heat-kernel theorem.**

Use `H_s H_t=H_(s+t)` for passive equality and the first Fourier mode for strict one-way Blackwell garbling. Derive strict inclusion of the soft mediator response sets using `0<rho<t-s` and the positive smooth preparation `H_rho`. Fix the mediator parameter experiment and heat geometry; any admitted relabeling must preserve that geometry and explicitly intertwine responses.

- [ ] **Step 7: Implement and run the standalone exact recomputation.**

The standard-library script must independently recompute BSC passive equality, soft-face diameters, the two exact determinants by rational elimination, the convex-mixture identity, and selected Fourier-coefficient inequalities with decimal values labeled corroborative. Run:

```powershell
& 'C:\Python314\python.exe' 'docs\derivations\2026-08-14-operational-intervention-extensions\evidence\recompute.py'
```

Persist deterministic sorted compact JSON plus one LF.

- [ ] **Step 8: Run checkpoint/static gates and commit.**

Run checkpoint validation, recomputation, package hash checks, LF/C0/BOM scans, and `git diff --check`; commit:

```powershell
git commit -m "docs: prove operational intervention extensions"
```

### Task 3: Assemble the rigorous-theory release certificate

**Files:**
- Modify: all nine required package root artifacts
- Create: `evidence/independent-reconstruction.md`
- Create: `evidence/oracle-erasure.md`
- Create: `evidence/adversarial-attacks.md`

**Interfaces:**
- Consumes: Task 2 proofs and exact output.
- Produces: a hash-bound release with atomic claims and adversarial coverage.

- [ ] **Step 1: Atomize the claim ledger and DAG.**

Separate quotient universality, finite minimality, compact topology, finite soft semantics, target-face BSC no-go, affine randomized no-go, Borel semantics, compact-Feller quotient, heat passive equality, Blackwell asymmetry, and scope exclusions. Keep raw realization canonicity outside the target closure.

- [ ] **Step 2: Populate at least nine mechanism families.**

Include raw kernel replacement, syntactic congruence, compact signature coding, target-colored soft faces, affine convolution, standard-Borel recursion, compact-Feller continuity, heat semigroup, and realization-category future work.

- [ ] **Step 3: Independently reconstruct and attack the theorem.**

Required attacks include universal-property direction, forgotten quotient maps, target erasure, non-affine randomized maps, correlated randomization, null conditional versions, lack of common domination, noncompact protocol spaces, latent reparameterization, boundary relabeling, response-image convexification, and ontology overreach.

- [ ] **Step 4: Bind every evidence artifact and release.**

Run the rigorous-theory release validator, an independent SHA-256 audit, JSON schema checks, semantic oracle erasure, LF/C0/BOM/final-newline scans, and `git diff --check`. Commit:

```powershell
git commit -m "docs: certify operational intervention extensions"
```

### Task 4: Integrate only released claims into central theory

**Files:**
- Modify: `Theory/05d_relational_inference.tex`
- Modify: `Theory/SPEC.md`
- Modify: `Theory/appendix_claim_ledger.tex`
- Modify: `overview.md`
- Modify: `docs/STATUS.md`
- Modify: `docs/research-plans/2026-08-12-elbo-to-continuum-action-worklog.md`

**Interfaces:**
- Consumes: Task 3 release and target digest.
- Produces: one consistent central theorem/status account.

- [ ] **Step 1: Add operational quotient minimality immediately after the existing behavioral quotient proposition.**

Use “response-compatible” and “contextually fully abstract”; state uniqueness over `A_P`; preserve latent/DAG realization as open.

- [ ] **Step 2: Add the soft/randomized and continuous sections before the existing category-independent open problem.**

Keep target-face preservation, affine randomization, compact-Feller hypotheses, and heat-kernel geometric typing visible in theorem statements.

- [ ] **Step 3: Propagate the scoped correction.**

Update SPEC, appendix, and overview in place. Append a new STATUS section and worklog section without rewriting chronology. Replace only the blanket “soft/stochastic/continuous remain open” wording; retain the target-erasing, correlated, noncompact, realization, agency, gauge/RG, VFE, and ontology obligations.

- [ ] **Step 4: Build and statically validate.**

Run the release validator and recomputation, label-uniqueness/status-vocabulary checks, UTF-8/EOL/C0/American-English scans, three `pdflatex` passes in a disposable `C:\tmp` output directory, and `git diff --check`. Commit:

```powershell
git commit -m "docs: integrate operational intervention extensions"
```

### Task 5: Independent review, formal closure, and publication

**Files:**
- Modify only findings-driven Task 2-4 files.
- Create/update ignored `.verification/ledger.json` for the final revision.

**Interfaces:**
- Consumes: complete feature branch.
- Produces: independent approval, formal closure, and published main parity.

- [ ] **Step 1: Dispatch independent reviewers.**

Use separate algebra/category, probability/topology, adversarial-counterexample, and manuscript-scope views. Treat the manuscript mathematics claims as high severity: record the `high_severity` escalation trigger, set `escalation_target` to four, require all four views, and link one structured verifier-skeptic before adjudication. Repair and re-review every Critical/High/Medium finding before closure.

- [ ] **Step 2: Run final evidence gates on the exact content commit.**

Run release validation, recomputation, evidence hashes, three-pass TeX, label/status/UTF-8/EOL/C0 scans, and `git diff --check`. Do not run pytest or model/GPU workloads.

- [ ] **Step 3: Create and validate the schema-1.1 closure ledger.**

Bind mathematics claims to current derivations and all four independent views, include one structured skeptic and one structured adjudicator linked to current evidence, and validate with the installed verification gate. Any tracked edit restarts closure.

- [ ] **Step 4: Publish without force.**

Fetch and require `origin/main` as an ancestor, explicitly push the feature branch, fast-forward the clean authoritative main checkout, rerun release/recomputation checks, push main, fetch, and prove local main, `origin/main`, and `git ls-remote` have the same SHA.

- [ ] **Step 5: Report a compact project map.**

Lead with four outcomes: what is now proved, what remains open, where the governing files are, and the exact published SHA. Offer Research-wiki ingest but do not write to the vault without confirmation.
