# Part II Renormalization Remediation Plan

**Goal:** Repair the accepted Part II findings for the fixed-$c_*$ decorated agent-network renormalization theory, verify the manuscript mechanically, and publish the result while preserving the live checkout's unrelated WIP.

**Authority:** `MultiAgentELBO/Theory` is the active source for this remediation. The Research vault is consulted only for context and is not modified.

## Scope

The implementation changes `07b_agent_network_rg.tex` and `07c_full_graph_meta_agent_vfe.tex`, then aligns `SPEC.md`, the notation appendix, the claim ledger, and repository provenance. The review report is retained under `docs/reviews/`, and the daily change log records the completed remediation. No Python runtime behavior or laboratory configuration changes.

## Remediation

1. Type the pointwise parent channel as a vertical channel in the fixed fiber above $c_*$ and keep section gluing, connection compatibility, and base-space coarse maps explicitly open.
2. Replace unnormalized overlapping indicators by normalized assignment kernels, retain joint event laws recursively, and state their composition law.
3. Reuse rooted tree transports, based holonomy, and dressed boundary links with explicit endpoint gauge laws.
4. Put adaptive graph data in one recognition-independent augmented structural channel applied to the generative, posterior, and recognition laws.
5. Keep the finite closure theorem conditional on equivariant measurable conditional versions and qualify cross-scale coupling by nontrivial parent dependence.
6. Align notation, status, provenance, and the durable review record.

## Verification

Compile `Theory/main.tex` with TeX Live into a temporary directory, compare compile diagnostics with the pre-edit baseline, scan for the retired undefined gauge legs and overclaims, inspect the complete diff, and verify Git references and preserved WIP after publication.
