# panelA-T-SIMUL-skeptic

*Recovered verbatim from workflow journal.jsonl, 2026-08-13. Agent a0335579.*

## survives

true

## confidence

high

## corrected_statement

SURVIVING THEOREM (T-SIMUL), with corrected hypotheses. Parts (A), (B), (C), (E) survive essentially intact; (D) survives as an identity but its headline inference is struck and its EM reading is refuted.

HYPOTHESES AS CORRECTED.
H1 unchanged (regular model, Theory/05c_pullback_geometry.tex:30-42). H2 unchanged, but its justification must be restated: frozen frames is a RESTRICTION, not a consequence of PIFB2.tex:963's eta_phi ~ eps^2 eta_q (slow velocity does not zero a derivative); and since Omega_ij = e^{phi_i}e^{-phi_j} reads the RECEIVER's frame, H2 freezes all frames, not only senders'. H3 REPLACED BY H3': for every active edge and every x_i in a neighborhood, sup_{x_i' in nbhd} ||s_{x_i'}|| <= G with E_{Omega_il q_l}[G] < infinity — local uniform score domination against the AUDIENCE measures. H1 does not supply this; it dominates only against the model's own reference measure. (Automatic in the deployed Gaussian/GL sector, where scores are affine.) H4 SPLIT: with u_ab redefined as the eps-floored second argument N(Omega mu_b, Omega Sigma_b Omega^T + eps I) — still a probability measure — part (D) is EXACT at eps > 0; only (B)'s closed form degrades to O(eps). H5 unchanged. H6 unchanged, plus: the conclusion is independent of the discretization, holding also for the delay ODE, Gauss-Seidel sweeps, and the dt-free exact-argmin update. H7 unchanged, plus: the isotropy condition is T in O(S) for the shared covariance S, not literally orthogonal. H8 unchanged, plus the new item: everything is stated for the ONE-channel skeleton; the two-channel case is a direct sum with per-channel b_i, qbar_i and a doubled edge-symmetry requirement.

(A) DIAGONAL IDENTITY. S = Phi o Delta and L(.|y) = Phi(.,y) agree on the whole diagonal. True by definition, and therefore carries no stationarity information: it constrains only derivatives ALONG the diagonal. The word "deployed" must be dropped — S is the tau=1, unit-private-coefficient, chi-free skeleton, not PIFB2's functional (PIFB2.tex:673 runs tau = kappa*sqrt(K_q); eq:free_energy_functional_final weights self and observation terms by chi_i).

(B) REACTION DECOMPOSITION. grad S = V^rec + R^react by the chain rule (needs no H2 — it survives co-evolving frames, since Omega_ij = Omega(phi_i, phi_j) is still a function of the two slots). Under H1, H2, H3', R^react_i = -b_i E_{qbar_i}[s_{x_i}], and in an exponential family in natural coordinates R^react_i = b_i(m_i - mbar_i). R^react_i = 0 iff E_{qbar_i}[s_{x_i}] = 0, i.e. x_i is a STATIONARY point of the pooled back-transported cross-entropy; this upgrades to "M-projection" only in an exponential family with mbar_i interior to the mean-parameter domain. For Gaussians it forces Sigma_i to equal the mixture covariance INCLUDING between-component dispersion, not the weighted average of transported covariances (verified: required eigenvalues 0.6125, 1.4414 vs weighted-average 0.3912, 1.2529) — matching PIFB2.tex:1602's "dispersion term included". Magnitudes verified: ||R^react|| = 61.7 against ||V^rec|| = 40.8, decomposition residual 7.1e-9.

(C) NON-BRIDGE. UNCHANGED AND STRENGTHENED. The lagged scheme converges at O(dt) to xdot = -V^rec, not to xdot = -grad S; the fields differ by R^react, which is O(1). On the 3-cycle eig(-J_lag) = {-(a+3b/2) +/- i b sqrt(3)/2, -a}, complex for every b > 0. STRENGTHENING: complex spectrum at an equilibrium excludes not only Euclidean C^2 gradient flows but ALL Riemannian gradient flows -G(x)^{-1} grad f with G SPD, since G^{-1} Hess f is similar to G^{-1/2}(Hess f)G^{-1/2}; this covers PIFB2's natural-gradient deployment (PIFB2.tex:967). ROBUSTNESS: the counterexample survives restriction to strictly positive row-stochastic (softmax-reachable) attention — max|Im| = 0.606, 0.693, 0.779, 0.823 at eps = 0.30, 0.20, 0.10, 0.05. And the load-bearing separation is dt-free: lagged fixed point (4/7, 1/7, 2/7) vs same-time (1/2, 1/4, 1/4). "Generically" should be weakened to "on an explicit open family". "Same-time PIFB2 is the dt -> 0 limit of the lagged ELBO-exact scheme" is FALSE.

(D) POSITIVE SETTLEMENT — IDENTITY KEPT, INFERENCE STRUCK, EM READING REFUTED.
KEPT: Lemma 5.1 (evidence invariance) is correct, and under H5 the identity S(x) = -log p(o) + KL(Q_x || P_{theta=x}(.|o)) holds for every x. EXTENDED (new): for tau >= 1, lambda_h >= 1 the DEPLOYED scalar still upper-bounds the same fixed number, since deployed - certified = (tau-1) sum_a KL(beta_a||pitilde_a) >= 0; the identity degrades to an inequality but the bound property reaches most of the deployed path. Also exact, not O(eps), under the H4 repair.
STRUCK: "so the deployed scalar is not merely an engineered consensus energy" does not follow. UNIVERSAL CERTIFICATION LEMMA: for ANY G(x) = sum_a KL(rho_a(x)||nu_a(x)) with rho_a, nu_a probability measures on any auxiliary space, appending likelihood-free coordinates with prior nu_a(theta) and recognition rho_a(x), tied at theta = x, gives F_private + G = -log p(o) + KL(Q_x||P_x(.|o)) with the SAME theta-free evidence. The author's construction is the special case. Hence R + cW is certified for every c in (0,1] — argmins (0.7143, 0.1429, 0.1429), (0.6, 0.2, 0.2), (0.5, 0.25, 0.25) at c = 1/4, 1/2, 1 — as are the reverse-KL consensus, wrong-transport consensus, and pure junk. The certification has no discriminating power and in particular cannot adjudicate between S and the half-coupled functional of (E). What survives is narrower but real: the peer-plus-attention block is EXACTLY a posterior-KL gap rather than merely bounded by one, so descent on it is monotone tightening of that gap. That is a statement about the FORM of the term, not evidence that the term is correct.
REFUTED: "the lagged theorem is exactly the E-step of this variational EM" is misleading. E-step-then-retie is not EM: retying theta = x^{n+1} is not the M-step (the M-step optimum requires q_a = u_ab, i.e. consensus), so EM monotonicity does not transfer. Computed with the canonical coordinate update of the lagged ELBO (N=4, a=0.3, row-stochastic beta, 80 steps, five trials): S INCREASES on 74/80, 71/80, 3/80, 5/80, 72/80 steps, and the limit sits at a strictly looser bound, excess 0.0097 to 0.2318 (3-cycle: 0.285714 vs 0.250000). Correct replacement: "the lagged theorem supplies the recognition-slot (E-step) force of the two-slot functional; R^react is its generative-parameter-slot force; the alternation E-step-plus-retie has no monotonicity property, and the ELBO-exact scheme provably stalls at a strictly looser bound than min S." Related conjecture, NOT a theorem: for row-normalized attention the infinitesimal lagged gradient flow does descend S (sym(MA) PSD to 5.6e-15 over an optimized N = 3..8 search, A = I - B, M = I + D_col - B - B^T); it is falsified for unnormalized beta by an explicit N=5 witness where S rises 14.5%.
Finally, Lemma 5.1's mechanism is already registered in the manuscript: Theory/04_generative.tex:163-166 (prop:gen-moving-target-witness) states that nonconstant Q-dependence "does not imply that e varies... or that no single member's evidence bounds the family of bounds", with a worked witness at 04:169-183. Lemma 5.1 instantiates that, it does not escape a standing obstruction.

(E) SHARED EQUILIBRIA. Unchanged and correct. The set-theoretic characterization is trivially valid (I checked both directions). The refutation of "shared equilibria iff reciprocal-symmetric" stands: under edge-symmetry R^react = V^{peer,rec}, so the peer force DOUBLES and V^rec = grad(R + W/2) (verified to 1.29e-9), giving lagged (2/3, 1/3) vs same-time (3/5, 2/5). SCOPE CORRECTION to the author's own softmax obstruction: the row-normalizer asymmetry is O(1/tau), not O(1) — max|beta* - beta*^T| = 0.4271, 0.2025, 0.0436, 0.0088 at tau = 0.7, 2, 10, 50 with exactly symmetric E and uniform pi — so at the deployed tau = kappa*sqrt(K_q) it is a small perturbation. The halving repair is nonetheless dead on the deployed path for the OTHER reason the author gives: GL transports on full covariances break energy edge-symmetry outright.

SECTION 6 (SANOV). Correct as written; I verified the Stirling expansion exactly, including the constant c(q) = ((A-1)/2)log(2*pi) + (1/2) sum_a log q_a and the o(1/M) remainder. The recommended re-scoping from "route to same-time coupling" to "route to a finite-M correction of the lagged coupling" should be adopted.

BOTTOM LINE. The refutation half of T-SIMUL is sound, robust, and stronger than the author claims. The positive half is a true identity whose interpretive load is overstated: it certifies a whole class of consensus energies equally, and the variational-EM gloss fails on monotonicity. The honest one-line settlement is: SAME-TIME PIFB2 minus LAGGED-ELBO = R^react, an O(1) audience-moment-mismatch force in the generative-parameter slot; the two schemes have different flows AND different equilibria; the same-time scalar is a valid upper bound on one fixed -log p(o), but so is every member of a large family of alternatives, so the bound status certifies well-formedness, not correctness, of the deployed coupling.

## attacks

### 1

{
  "attack": "1. HIDDEN HYPOTHESIS \u2014 domination for differentiating under the integral against a FOREIGN measure; pointwise-vs-open-set closedness; M-projection outside exponential families",
  "verdict": "REPAIRABLE",
  "reasoning": "THREE real gaps, none fatal.\n\n(a) The load-bearing interchange in \u00a72.4 is d_{x_i} \u222b q_l(z) log(Omega_li q_{x_i})(z) dz = \u222b (Omega_il q_l)(w) s_{x_i}(w) dw. I re-derived it and it is correct as an identity (change of variables w = T_li^{-1} z, dz = |det DT_li(w)| dw; the density of (T_il)_# q_l at w is q_l(T_li w)|det DT_li(w)|; the Jacobian is x_i-free and cancels). But the INTERCHANGE needs a dominating function integrable against Omega_il q_l \u2014 the AUDIENCE measure \u2014 i.e. sup_{x_i in nbhd} ||s_{x_i}(w)|| <= G(w) with E_{Omega_il q_l}[G] < infinity. H1 does not supply this. I read its source: Theory/05c_pullback_geometry.tex:30-42 (hyp:pb-regular-models) grants 'third-power integrability of every score direction' and 'domination sufficient to differentiate the divergence expressions below through third order' \u2014 all under the model's OWN law p, not under a foreign measure. H3 supplies only pointwise finiteness E_{Omega_il q_l}||s_{x_i}|| < infinity, which is one-x finiteness, not local uniformity. This is a genuine hole in a hypothesis the author explicitly claims to have instantiated from the manuscript. Fix: replace H3 by H3' (local uniform score domination against every active audience measure). For the deployed Gaussian/GL case scores are affine and H3' is automatic, so nothing downstream changes.\n\n(b) \u00a72.5 states 'AT A CONSENSUS CONFIGURATION, THE LAGGED FIELD IS CLOSED IFF THE ATTENTION MATRIX IS SYMMETRIC THERE'. Closedness of a 1-form AT A POINT is vacuous \u2014 Poincar\u00e9 needs d(omega)=0 on an OPEN set, and the transported-consensus locus {q_i = Omega_ik q_k} is a positive-codimension submanifold. The correct statement is Jacobian-block symmetry at that point, which yields no potential. Wording slip, not a math error.\n\n(c) '(B) ... R^react_i = 0 iff q_{x_i} is the M-projection' is exponential-family-specific. In general R^react_i = 0 only says E_{qbar_i}[s_{x_i}] = 0, i.e. x_i is a STATIONARY point of the pooled cross-entropy x_i |-> -E_{qbar_i}[log q_{x_i}], not a maximizer. It is an M-projection only when the log-likelihood is concave in the coordinate (exponential family, natural parameters) and mbar_i lies in the interior of the mean-parameter domain. Add that hypothesis.\n\nI verified the closed form is otherwise exactly right, including the subtlety the author flags via PIFB2.tex:1602. For Gaussians T=(w,ww^T), so R^react_i = 0 forces Sigma_i = mixture covariance INCLUDING between-component dispersion, NOT the weighted average of transported covariances. Computed: required Sigma eigenvalues (0.6125, 1.4414) vs weighted-average-of-covariances eigenvalues (0.3912, 1.2529); matching means alone leaves second-moment residual 0.6207. This is exactly PIFB2.tex:1602's 'dispersion term included' barycenter, so the cross-check the author claims is real."
}

### 2

{
  "attack": "2. ORDER OF LIMITS \u2014 is dt -> 0 illegitimately swapped with the lag, a sum, or the argmin?",
  "verdict": "FAILS_TO_LAND",
  "reasoning": "I attacked (C) from every ordering I could construct and it holds each time.\n\n(i) Fixed physical lag: take L*dt -> T_lag > 0 instead of L*dt -> 0. You get the delay ODE xdot(t) = -grad_1 Phi(x(t), x(t - T_lag)), whose T_lag -> 0 limit is -grad_1 Phi(x,x) = -V^rec. Same answer.\n(ii) Gauss-Seidel sweep instead of Jacobi: agent i uses already-updated x_1..x_{i-1}. The DIFFERENTIATION SLOT is unchanged (still slot 1 only), so the dt -> 0 limit is again -V^rec.\n(iii) No dt at all: the tied-replica theorem's natural update is the exact coordinate minimizer of L(.|q^n), x_i <- (a m_i + sum_j beta_ij x_j^n)/(a + sum_j beta_ij). Its fixed-point set is exactly {V^rec = 0}, unchanged.\n\nCritically, the load-bearing content of (C) is dt-FREE: the equilibrium separation. I recomputed both by hand and numerically for the 3-cycle (a=b=1, m=(1,0,0)): lagged fixed point (4/7, 1/7, 2/7) = (0.571429, 0.142857, 0.285714), same-time critical point (1/2, 1/4, 1/4). R^react at the lagged fixed point = (2/7, -3/7, 1/7) = (0.285714, -0.428571, 0.142857), nonzero. All three match the author exactly.\n\nI also reproduced the convergence table (T=3, x0=(0.3,-0.8,0.5)):\n  dt=0.100  |lagEuler-lagODE| 4.29e-3   |lagEuler-simEuler| 1.335233e-1\n  dt=0.050  2.16e-3   1.333651e-1\n  dt=0.020  8.66e-4   1.332527e-1\n  dt=0.010  4.33e-4   1.332131e-1\n  dt=0.005  2.17e-4   1.331929e-1\n  dt=0.002  8.67e-5   1.331807e-1\nClean O(dt) to its own limit; the same-time gap is frozen at 0.1332 and does not shrink. The author's first column differs in the third digit (he reports 4.95e-3 at dt=0.1) \u2014 a reference-trajectory convention difference, immaterial. No illegitimate swap found."
}

### 3

{
  "attack": "3. ASYMMETRY OF KL \u2014 was a second-order expansion used, was the third-order term waved away, does argument order matter?",
  "verdict": "FAILS_TO_LAND",
  "reasoning": "No KL expansion is used anywhere in the derivation; every derivative is exact. I independently re-derived the two claims where the asymmetry could hide, and both are correct.\n\n(i) The closedness criterion's score form. E_ik(x_i,x_k) = KL(q_{x_i} || Omega_ik q_{x_k}). d_1 = \u222b q_i s_i log(q_i / Omega_ik q_k); then d_2 gives -\u222b q_i s_i sigma_k^T = -E_{q_i}[s_i sigma_k^T] = -Cov_{q_i}(s_i, sigma_k), using E_{q_i}[s_i] = 0. For the reverse edge, E_ki(x_k,x_i) = KL(q_{x_k} || Omega_ki q_{x_i}), d_1 in x_k then d_2 in x_i gives -\u222b q_k(z) s_k(z) s_{x_i}(T_ik z)^T dz; changing variables w = T_ik z turns this into -E_{Omega_ik q_k}[sigma_k s_i^T], and E_{Omega_ik q_k}[sigma_k] = \u222b q_k s_k = 0, so it IS a covariance under Omega_ik q_k. The author's assertion \u2014 two cross-covariances under DIFFERENT reference measures \u2014 is exactly right (up to transpose bookkeeping, which the Monderer\u2013Shapley block condition already carries). This IS where the KL asymmetry lives and he located it correctly.\n\n(ii) H7's reduction. Edge-symmetry beta_ij E_ij(u,v) = beta_ji E_ji(v,u) with beta_ij = beta_ji requires KL(q_u || Omega_ij q_v) = KL(q_v || Omega_ji q_u). Since T_ij is a bijection and KL is invariant under a common bimeasurable pushforward, KL(q_v || Omega_ij^{-1} q_u) = KL(Omega_ij q_v || q_u). So the condition is exactly forward KL = reverse KL between q_u and Omega_ij q_v. The author's statement \u2014 Gaussians only in the mean-only sector with common covariance and covariance-preserving transports \u2014 is correct (strictly, T in the isotropy group O(S) of the shared covariance S, not literally 'orthogonal', which is the S = I case).\n\nOne residue only: 2.5's collapse 'the condition becomes beta_ik = beta_ki' assumes the common cross-covariance is nondegenerate. Asserted, not checked. It is generically true (affine scores in the Gaussian sector) but should be stated.\n\nMonderer & Shapley 1996 Thm 4.5 is cited correctly and the extension from interval strategy sets to R^{d_i} blocks is legitimate: splitting player i into d_i clones with the shared payoff u_i adds only the trivially-satisfied same-player conditions, so the criterion reduces to the displayed cross-block condition."
}

### 4

{
  "attack": "4. NORMALIZATION \u2014 does the constructed law integrate to 1, was a partition function dropped?",
  "verdict": "FAILS_TO_LAND",
  "reasoning": "Lemma 5.1 is a correct two-line integration and I re-did it. The copy blocks integrate out because the pi rows are probability vectors and every u^n_{ab} = (Omega_ab)_# q_b, v^n_{ab} = (OmegaTilde_ab)_# s_b is a pushforward of a probability measure by a bijection, hence a probability measure. No normalizer is dropped: p^n(o) = prod_a \u222b p_a(dk) r_a(dm) L_a(o_a|k,m) * 1 * 1.\n\nThe softmax normalizer is also handled correctly. I re-derived the envelope value: min over the row simplex of sum_j beta_ij E_ij + tau sum_j beta_ij log(beta_ij/pitilde_ij) = -tau log Z_i with Z_i = sum_j pitilde_ij e^{-E_ij/tau}, and the value substitutes cleanly (the E-terms cancel against -tau*beta*log beta). Then \u00a73.2's Hessian:\n  Hess T = sum_{ij} beta*_ij Hess E_ij - (1/tau) sum_i Cov_{beta*_i}(grad E_{i.}, grad E_{i.}).\nVERIFIED numerically (N=3, K=2, orthogonal transports, tau=0.6): ||H_num - H_analytic||_inf = 5.28e-7 against ||H|| = 5.794; ||H_num - H_num^T||_inf = 4.44e-7; min eigenvalue of a softmax-response covariance block = -1.42e-16 (PSD to machine precision). Both terms manifestly symmetric, response term enters with a minus sign as claimed.\n\nI also found H4 is WEAKER THAN NECESSARY \u2014 a repair in the author's favor. PIFB2.tex:187 records that the implementation adds eps*I to the SECOND-argument covariance. That second argument, N(Omega mu_j, Omega Sigma_j Omega^T + eps I), is still a bona fide probability measure. So define u_ab as that blurred measure; then Lemma 5.1 and identity (D) hold EXACTLY at eps > 0, not merely to O(eps). Only (B)'s closed form genuinely degrades to O(eps), because the blur breaks u_ab = (Omega_ab)_# q_b and hence breaks the score identity. H4 should be split accordingly."
}

### 5

{
  "attack": "5. LEVEL CONFUSION \u2014 is a recognition variable being treated as a generative one?",
  "verdict": "WEAKENS_SCOPE",
  "reasoning": "Yes, and the author fences it, but he understates how much the manuscript already anticipates.\n\nUnder the tie, q_a plays three roles simultaneously: (i) recognition marginal over the private latent K_a; (ii) recognition law over the copy coordinate X_a; (iii) via u_ab = Omega_ab q_b, the GENERATIVE source law on agent a's copy coordinate. Role (iii) is precisely what Theory/04_generative.tex:126-128 calls out: 'A transition built from a live peer recognition marginal is a map from recognition laws to kernels, not a factor of a joint fixed before inference.' So req:gen-typing-prohibition (04:120-124) is satisfied by the UNTIED family {P_theta} but violated by the tied object. The author's defense \u2014 the tie is a constraint on the optimization, not a typing violation \u2014 is exactly the defense prop:gen-no-distinguished-target already anticipates, and the manuscript's own verdict on such families is 04:143: 'The family itself selects no joint as \"the model.\"'\n\nSharper: Lemma 5.1 is not a new escape. Theory/04_generative.tex:163-166 (prop:gen-moving-target-witness) ALREADY states 'Nonconstant dependence of P_{theta,Q} on Q does not imply that e varies, that no ascent can improve a fixed quantity, or that no single member's evidence bounds the family of bounds', with a worked binary witness at 04:169-183 where e(Q_beta) = log(1/2) for every beta. Lemma 5.1 instantiates exactly that already-registered phenomenon in the tied-replica inventory. It is a correct and useful instantiation, but the framing 'Lemma 5.1 pays the price' overstates its novelty relative to the manuscript.\n\nSecond level issue: the copy coordinate X_a and the private latent K_a live on the same space but are DIFFERENT random variables in the joint, and the likelihood L_a(o_a|k_a,m_a) reads K_a only. So the consensus energy is attached to a replica that is causally disconnected from the data. That is the mechanism behind Lemma 5.1 and it is structural, not lucky \u2014 which is what attack 6 exploits."
}

### 6

{
  "attack": "6. TAUTOLOGY \u2014 is any 'identity' true only by definition of one of its terms?",
  "verdict": "WEAKENS_SCOPE",
  "reasoning": "THIS IS THE STRONGEST HIT. (A) is a definition (conceded). (E)'s first sentence is pure set logic (A = B iff their difference vanishes on the union \u2014 I checked both directions; correct and content-free). The serious casualty is (D)(i).\n\nUNIVERSAL CERTIFICATION LEMMA (my construction; DERIVATION). Let G(x) = sum_a KL(rho_a(x) || nu_a(x)) where rho_a(x), nu_a(x) are ANY probability measures on ANY auxiliary space Z_a, both allowed to depend on x. Append coordinates (Z_a)_a to the private generative model with prior prod_a nu_a(theta) and NO observation factor; take recognition prod_a rho_a(x); tie theta = x. Then p_theta(o) = p_private(o) exactly (theta-free, by the same two-line integration as Lemma 5.1), the posterior on Z factorizes as the prior, and\n  F_private(x) + G(x) = -log p(o) + KL(Q_x || P_x(.|o))   for every x.\nThe author's construction is the special case Z_a = (J^q_a, X_a), nu_a = pi^q_a (x) u_a(theta), rho_a = beta_a (x) q_a.\n\nCONSEQUENCE: the certification has ZERO discriminating power. With the SAME fixed -log p(o) it equally certifies (all at frozen beta, all realized as legitimate normalized label blocks):\n  * R + cW for every c in (0,1] \u2014 realize with peer weights c*beta_ij plus a self-source j=i of weight 1-c whose energy is KL(q_i||q_i) = 0, keeping the row a probability vector;\n  * the REVERSE-KL consensus sum beta_ij KL(Omega_ij q_j || q_i);\n  * a wrong-transport consensus, or pure junk such as sum_a KL(q_a || N(0,I)).\nCOMPUTED on the 3-cycle: argmin(R + cW) = (0.714286, 0.142857, 0.142857) for c=1/4, (0.6, 0.2, 0.2) for c=1/2, (0.5, 0.25, 0.25) for c=1. Three different points. Each is 'a genuine upper bound on ONE FIXED number', each has 'descent = genuine tightening of the posterior-KL gap'.\n\nSo (D)'s clause 'the deployed same-time PIFB2 scalar ... is not merely an engineered consensus energy' DOES NOT FOLLOW. The argument certifies engineered consensus energies as a CLASS. In particular it cannot adjudicate between S and the half-coupled R + W/2 that (E) says the lagged flow descends \u2014 both are exact ELBOs on the same fixed number, with different minimizers. The identity in (D) is TRUE; the inference drawn from it is not. What is genuinely non-tautological in (D) is narrower and worth keeping: the peer + attention block is EXACTLY a KL gap (not merely bounded by one), so descent on it is monotone tightening \u2014 but that is a statement about the FORM of the term, not evidence that the term is right."
}

### 7

{
  "attack": "7. SCOPE INFLATION \u2014 does the theorem quantify over more than the proof delivers?",
  "verdict": "WEAKENS_SCOPE",
  "reasoning": "Three overreaches, one repairable in the author's favor.\n\n(a) THE WORD 'DEPLOYED' IS UNEARNED, in both (A) and (D). H8 excludes tau != 1, non-unit private coefficients, alpha_i(c), chi weighting, base-gradient and curvature sectors. But PIFB2.tex:673 states the working implementation runs tau = kappa*sqrt(K_q) (belief fiber) and kappa*sqrt(K_m) (model fiber), and PIFB2's canonical functional (eq:free_energy_functional_final) weights the self-KL and observation terms by chi_i(c) while the peer terms carry chi only inside pitilde. So S := Phi o Delta is NOT the deployed scalar; it is the tau=1, unit-coefficient, chi-free skeleton. Every occurrence of 'deployed' should read 'the tau=1 unit-coefficient two-channel skeleton'.\n\nREPAIR I can offer (the author does not): the BOUND survives tau != 1 as an INEQUALITY. Deployed minus certified = (tau - 1) * sum_a KL(beta_a || pitilde_a) >= 0 whenever tau >= 1, so for tau >= 1 the deployed scalar still upper-bounds the same fixed -log p(o) \u2014 it just is not an exact gap. Since tau = kappa*sqrt(K_q) with K_q up to 90 in the sweep, tau >= 1 is the operating regime. Same argument for lambda_h >= 1 on the model channel; and with chi_i in [0,1] fixed the bounded number becomes sum_i chi_i(-log p_i(o)), still one fixed number. So (D)(i) upgrades from 'exact identity on a skeleton' to 'valid inequality on most of the deployed path'. That is a real strengthening the author left on the table.\n\n(b) ONE-CHANNEL Phi vs TWO-CHANNEL theorem. The closed theorem carries a belief block (beta, q, Omega) AND a model block (gamma, s, OmegaTilde) with two entropy blocks. Phi has one peer block. The extension is by direct sum, but then (B) has TWO reaction terms with per-channel b_i and qbar_i, and (E)'s edge-symmetry must hold on BOTH channels simultaneously \u2014 strictly harder than stated.\n\n(c) 'V^rec is GENERICALLY not the gradient of any C^2 potential' \u2014 'generically' is not proved. What is proved is nonintegrability on an explicit family (the 3-cycle and a neighborhood of it, see attack 8) plus one numerical antisymmetry measurement. That suffices for the refutation in (C) but not for the word 'generically'. Note also PIFB2.tex:2430 already asserts the receiver-only field 'is not the gradient of S_surr and is generically nonintegrable', so (C)'s nonintegrability is not new \u2014 the new content is only the identification V^rec = the lagged-ELBO field.\n\n(d) One admitted obstruction is OVERSTATED, against the author. He says live softmax 'KILLS the halving repair'. It does, but the effect is O(1/tau). With exactly symmetric E and uniform pi at N=4, K=3 I get max|beta* - beta*^T| = 0.4271 (tau=0.7), 0.2025 (tau=2), 0.0436 (tau=10), 0.0088 (tau=50) \u2014 clean 1/tau decay. At the deployed tau = kappa*sqrt(K_q) this is a small perturbation, not an O(1) kill. (The halving repair is still dead on the deployed path, but for the OTHER reason he gives: GL transports on full covariances break edge-symmetry of the energies outright.)\n\n(e) H2's operational justification is a category error. PIFB2.tex:963 gives eta_phi ~ eps^2 eta_q \u2014 that makes the frame VELOCITY small, not the frame-slot DERIVATIVE zero. Frozen frames is a legitimate restriction; citing a timescale ratio as its justification is not. Separately, since PIFB2's transports are Omega_ij = e^{phi_i}e^{-phi_j}, they depend on the RECEIVER's frame too, so H2 requires freezing all frames, not just senders'."
}

### 8

{
  "attack": "8. EXPLICIT COUNTEREXAMPLE \u2014 try to kill (C), (B), (E); and test (D)(iii)",
  "verdict": "WEAKENS_SCOPE",
  "reasoning": "I could not break (A), (B), (C) or (E). I DID break (D)(iii).\n\nWHAT SURVIVED, AND WHY MY ATTACKS FAILED:\n* (B) decomposition: recomputed independently on FULL Gaussians (N=3, K=2, mean + Cholesky, random GL transports, random asymmetric beta). ||grad S - (V^rec + R^react)||_inf = 7.1e-9 with ||grad S|| = 74.04, ||V^rec|| = 40.80, ||R^react|| = 61.68. R^react is not a perturbation. Relative antisymmetry of Jac(V^rec) = 1.074; of Jac(grad S) = 6.7e-6. Confirmed.\n* (C) 3-cycle: eig(-J_lag) = {-2.5 +/- 0.866025i, -1}, eig(-J_sim) = {-4, -4, -1}. Exactly as claimed.\n* MY BEST ATTACK ON (C) FAILED. Objection: a directed 3-cycle with hard zeros is not softmax-reachable (softmax gives strictly positive rows). I perturbed to strictly positive ROW-STOCHASTIC B = (1-eps)P + eps(J-I)/2: eig(-J_lag) stays complex at eps = 0.30, 0.20, 0.10, 0.05 with max|Im| = 0.6062, 0.6928, 0.7794, 0.8227. The counterexample is robust inside the softmax-reachable set.\n* I ALSO STRENGTHENED (C). The author's argument as written only excludes EUCLIDEAN gradient flows. But PIFB2.tex:967 deploys natural-gradient flow (Sigma-sandwich preconditioning). Any Riemannian gradient flow xdot = -G(x)^{-1} grad f with G SPD linearizes AT AN EQUILIBRIUM to -G^{-1} Hess f, which is similar to -G^{-1/2}(Hess f)G^{-1/2}, symmetric, hence REAL spectrum. So complex eigenvalues exclude natural-gradient/Fisher-preconditioned gradient flows too. (C) is stronger than stated.\n* (E) halving: ||V^peer,rec - (1/2) grad W||_inf = 1.29e-9 at N=4, K=3, orthogonal coboundary transports, symmetric beta (||grad W|| = 14.71). N=2 equilibria (2/3, 1/3) lagged vs (3/5, 2/5) same-time verified by hand and numerically. Confirmed.\n\nMY COUNTEREXAMPLE AGAINST (D)(iii) \u2014 'the lagged theorem is exactly the E-step of this variational EM':\nThe composite 'E-step then RETIE theta = x^{n+1}' is NOT EM and lacks EM's monotonicity, because retying is not the M-step (the M-step minimizer over theta requires q_a = u_ab, i.e. consensus). Consequence: the ELBO-exact lagged scheme can INCREASE the very bound (D) says it tightens. Computed with the canonical coordinate update of the lagged ELBO, x_i <- (a m_i + sum_j beta_ij x_j^n)/(a + sum_j beta_ij), N=4, a=0.3, row-stochastic beta, 80 iterations, five random trials:\n  #S-increases = 74/80, 71/80, 3/80, 5/80, 72/80\n  bound excess at the limit S(lagged fp) - min S = 0.0628, 0.0344, 0.0319, 0.0097, 0.2318\nSo (i) the exact ELBO update is strongly non-monotone in S, and (ii) it always stalls at a STRICTLY looser bound. On the 3-cycle: S(lagged fp) = 0.285714 vs min S = 0.250000. In EM language: the E-step direction is a descent direction for L(.|y) at frozen y, not for S, and the retie can undo the step.\nI also built an explicit ascent witness for the infinitesimal lagged GRADIENT flow: N=5, unnormalized beta (row sums 60.04, 0.667, 16.21, 4.05, 0.105), S rises from 6.35654 to a peak of 7.28029 (+14.5%), 1052/2000 steps increasing.\nHONEST LIMIT OF THIS ATTACK: for ROW-STOCHASTIC (softmax-normalized) beta I could NOT produce gradient-flow ascent. Writing A = I - B and M = I + D_col - B - B^T (so V^rec = a(x-m) + Ax, grad S = a(x-m) + Mx), an optimized search over N = 3..8 gives min lambda_min(sym(MA)) = -5.56e-15, i.e. sym(MA) is PSD to machine precision. So the following is a CONJECTURE, not a theorem: with row-normalized attention the lagged gradient flow descends S (to the wrong point). The non-monotonicity is then a finite-step-size phenomenon \u2014 but the tied-replica theorem licenses the exact coordinate update, which IS finite-step, so the counterexample stands where it matters.\n\nSEPARATELY, \u00a76 SURVIVES INTACT \u2014 I checked it rather than assuming it. The quenched finite-M Sanov expansion is exactly right. Method of types plus Stirling: log Pr(L_M = q) = log M! - sum_a log(Mq_a)! + M sum_a q_a log p_a = -M KL(q||p) - ((A-1)/2) log(2*pi*M) - (1/2) sum_a log q_a + O(1/M). Hence -(1/M) log Pr = KL(q||p) + ((A-1)/(2M)) log M + (1/M)c(q) + o(1/M) with c(q) = ((A-1)/2) log(2*pi) + (1/2) sum_a log q_a, and sum_a log q_a = -log det of the multinomial Fisher form up to sign \u2014 so 'explicit Stirling/Fisher-determinant constant' is accurate. The o(1/M) is right because Stirling's next term contributes O(1/M) to log Pr, i.e. O(1/M^2) after dividing. The re-scoping recommendation (finite-M correction to the LAGGED coupling, not a route to same-time reciprocity) is correct and should be adopted."
}

