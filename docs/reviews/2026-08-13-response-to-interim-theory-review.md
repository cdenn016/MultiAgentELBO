# Response to Referee — `docs/reviews/2026-08-12-pifb2-elbo-program-interim-theory-review.md`

**Reviewed revision** `caa4a15` · **Adjudicated against** `4dee0db` (adds worklog §4b and the 16 verbatim panel returns at `docs/audits/panels-2026-08-12/`, neither visible to the reviewer) · Every adjudication below was independently re-verified; where a verifier overturned or narrowed an adjudication, the verifier's verdict governs and is marked.

---

## 1. Headline judgement

**This reviewer is worth taking seriously, and the review should be accepted as a major-revision report essentially as filed.** Twelve separable findings (8 numbered + 4 minor). Not one is wrong on its substance. Ten are upheld outright, two are narrowed, none is moot in the sense of "already repaired" — every defect the reviewer names is still live in the repository at `4dee0db`. `overview.md` is byte-identical between `caa4a15` and `4dee0db` (`git diff caa4a15 4dee0db -- overview.md` is empty), so every overview-level overclaim the reviewer flags is unrepaired.

Every load-bearing number in the review reproduces. Independent recomputation confirmed: the Jensen gap `log2 − ½log3 = 0.1438410362` (sympy-exact); the `arccos(cos Θ)` identity in the U(1) script to 4.4e-16 and the `Θ=π/2` vs `3π/2` record-law gauge-equivalence to 1.06e-16; the `(tr X)(tr Y)` counterexample (signature `(1,0,3)`/`(1,0,8)`, Ad-invariant to 2.8e-14); the oriented-vs-symmetric stencil rates `O(h)` vs `O(h²)` in both flat and covariant settings; and the `d=1 → 0`, `d=2 → finite`, `d=3 → divergent` scaling of the unweighted bond sum. Both repository scripts run clean.

**Two blind hits deserve explicit acknowledgement.** The reviewer wrote a one-sentence prediction in Finding 6 — that Fisher dressing would supply *a nonnegative invariant state-dependent sector that is nevertheless stabilizer-degenerate and noncoercive along noncompact gauge orbits* — without access to the T-CURV panel. That is, three for three, the entire T-CURV outcome (`panelA-T-CURV-derivation.md:23-25, :31, :35`; sharpened at `panelA-T-CURV-skeptic.md:33`). And Finding 2's lagged-vs-contemporaneous dichotomy was restated independently and blind at `panelA-T-GRAD-derivation.md:328, :351`, then converted into a proven inequivalence by §4.5 (T-SIMUL). A referee who predicts your next two panel results from the outside is calibrated.

**Where the reviewer is weakest.** (a) He repeatedly under-arms his own case: Finding 2's Bernoulli counterexamples refute only a pointwise reading of the *iff*, when the programme's own witness (`typed-construction.md:71-88`) refutes it under every reading; Finding 1's two-point model is under-specified (the quoted digits require uniform weights — `gap(w) = log(3^{w-1}(3-2w))` gives 0.0923 at `w=1/4`). (b) He misses internal prior art: Finding 8 was already filed at severity **HIGH** by `rm-05-provenance-gates.md:69, :138-140` *in this repository*, with a stronger diagnosis and a prescribed repair; Finding 1's well-posedness form was already stated at `docs/research-plans/2026-08-12-pifb2-continuum-roadmap.md:104` and confirmed at `rm-03-action-class.md:363-365`, both visible at `caa4a15`. (c) One remedy is mis-prescribed (Finding 5 — see below). (d) One claim over-reaches (Finding 7 — SPEC:20-25 carves out exactly the two constructions this programme wants to export).

**Bottom line for the PI:** the review is a claim-hygiene audit of unusual accuracy. It does not find a broken theorem — the finite tied-replica ELBO theorem, the KL expansion, the mutual-information correction, the scoped moving-peer no-go and the U(1) existence witness all survive, and I re-verified each. What it finds is that `overview.md` and the worklog state those results *one register stronger than they are*, in eight separate places, several of which the programme's own audits had already flagged internally and never propagated to the public surface. That is a real and correctable failure of the write gate, not of the mathematics.

---

## 2. Per-finding verdict table

| # | Finding | Final verdict | Severity | One-line justification |
|---|---|---|---|---|
| 1 | Theorem proves a repaired scalar, not equality to literal PIFB2 | **UPHELD** (status corrected: *confirmed, internally registered in 4 places, unfenced in the public overview*) | **High** | The theorem's term is `−E_ζ log ℓ(o|K,M)`; PIFB2's boxed display (`PIFB2.tex:689`) leaves `m_i` unbound and not among the functional's declared arguments (`:684`), so equality is not merely unproved — it is not well-posed. `overview.md:124` still asserts it unfenced. |
| 2 | Base-neighbour proposition conflates lagged with contemporaneous | **UPHELD** | **High** | An explicit exact-ELBO instance with a strictly design-product generative law (total correlation 7.4e-17) carries a strictly positive base-neighbour transported-KL sector — refuting `worklog:291`'s "⇐" and the load-bearing inference at `:250`, and by inheritance the boxed `:310-311` and `overview.md:171-173`. |
| 3 | Exact-contraction arrow is a conditional schema | **UPHELD, NARROWED** | Medium | The three named evidentiary gaps are now closed by §4.3 / `panelA-T-RESID-derivation.md:120-190`; the *conclusion* is strengthened, because the skeptic returns `survives:false` and the `ε_h = 0` case is `C_h = identity`. `overview.md:106` still reads "**EXACT** identity" with no conditions. |
| 4 | Local KL expansion ≠ global continuum estimate | **UPHELD, NARROWED** | **Medium** (verifier corrected up from *low*) | Sub-claim (5) superseded by §4.1(i); (2)(3)(4) answered by the T-GRAD panel; (6) and (7) confirmed numerically. Rated medium because `rm-06-prior-art.md:277` prices the missing piece — Γ-convergence for manifold-valued graph Dirichlet energies with a gauge connection — as *"the most expensive item in the roadmap."* |
| 5 | U(1) witness detects flat monodromy, not curvature or topology | **UPHELD** | Medium | `F = dA ≡ 0` on *any* 1-manifold, `H²(S¹;Z) = 0`; the implemented statistic is `arccos(cos Θ)`; and the `π/2` / `3π/2` **record laws** are gauge-equivalent to 1.06e-16, so `worklog:544-545`'s "the statistic *is* the holonomy" and `overview.md:182` are false as implemented. |
| 6 | Full-GL invariant-form no-go worded too strongly | **UPHELD** | Medium | `(tr X)(tr Y)` is Ad-invariant, nonnegative, rank 1 with radical `sl(K,R)` — so "**every** element is indefinite" (`worklog:156`, `overview.md:153`) is false, though the operative conclusion (no *positive-definite* Ad-invariant form) is true and complete. |
| 7 | Integration into `Theory/` conflicts with its SPEC | **PARTLY UPHELD** | Low–Medium | The reviewer quotes `SPEC.md:17` and stops; `SPEC.md:20-25` explicitly licenses the source-label attention derivation and the holonomy-conditioned projection. SPEC:17 is currently *honored* (zero PIFB references across the 20 chapters; `PIFB2.tex` not `\input` by `main.tex`). The real hole is that worklog §6's write policy (`:1128-1131`) never names `Theory/`. |
| 8 | Decision-packet closure provenance | **UPHELD in full; MOOT AS NOVELTY** | Medium (rm-05 rates it **HIGH**) | `final-report.md:14` cites an unnamed ledger; its own `claim-ledger.json` has `assumptions: []`, `evidence: []`, one `INCONCLUSIVE` target with `evidence_ids: []`. Correct redirection to `live-peer-fixed-joint` (REFUTED, `ev-counterexamples`). But `rm-05:138-140` already found this, ran the grep, and prescribed the exact repair. Unrepaired. |
| m-i | "finite-lattice" vs "finite-site" | **UPHELD** | Low | `worklog:24` / `overview.md:115` declare only a finite agent-site set; no edge set, adjacency, orientation, plaquette or mesh parameter appears in the theorem. The word is *inherited* from the effective-section-action artifacts, so the fix must propagate upstream. |
| m-ii | `exp(−L(γ)/ξ)` weight | **UPHELD, and understated** | Low–Medium | `05c` `prop:pb-curve-taxonomy` (ESTABLISHED): ω-horizontal ⇒ `L^ω(Γ) = 0`. §3d.1's transport *is* the horizontal lift, so the weight is not merely underived — it is identically 1 for every curve, every ξ. |
| m-iii | §3d.7 contradicts §3d.8 | **UPHELD, NARROWED** | Low | The `Status: CONJECTURE … not that a separating pair of connections exists` header at `:576-578` sits directly below a witness whose four checks pass. But obligations 1–2 *are* marked discharged inside §3d.8 (`:558-559, :571-572`) and §4b's revised list at `:681` already narrows the live set to 3–4. Residual defect: the stale header and the inverted §3d.8/§3d.7 ordering. |
| m-iv | Stale fragmentation table | **UPHELD** | Low | `overview.md:222-232` and `:229` ("exist in only one copy") are false at both revisions; the commit that falsified them is `caa4a15` itself. The reviewer missed that the worklog carries the same staleness twice (`:143` absolute `Documents/ChatGPT` path; `:179-184` table) in direct contradiction with `worklog:650-651`. |

---

## 3. Findings that demand an edit — concrete, actionable

### E1 — `overview.md:123-125` and its fence list `:127-133` (Finding 1) — **blocking**

Current: *"the lagged, unit-temperature, unit-coefficient two-channel PIFB2 action is exactly a negative ELBO"*, with a four-bullet "Reading of the terms" list that fences the tied-replica inventory, the lag, `τ=1` and the omitted `I(K;M)` — but is silent on the observation term. **Add a fifth bullet:**

> - The observation term is `−E_{ζ_a} log L_a(o_a | K_a, M_a)`, an expectation under the **joint** private law. PIFB2's boxed display (`Theory/PIFB2.tex:689`) writes `E_{q_i(c)}[log p(o(c)|k_i,m_i)]` with `m_i` unbound and not among the functional's declared arguments (`:684`); its pointwise display (`:669`) drops `m_i` entirely. **Equality to the literal PIFB2 observation term is not proved and is not currently well-posed.** The joint typing is the one the ELBO forces: `KL(ζ‖p⊗r) = KL(q‖p) + KL(s‖r) + I_ζ(K;M)` (verified to 1.1e-16). If `:669` is read as the predictive marginal `−E_q log ∫L(o|k,m)s(dm)`, the two differ by `E_q[D_KL(s ‖ s^{(o,k)})] ≥ 0` — the theorem's scalar is an **upper bound**, and the gap is unbounded in the model uncertainty (Gaussian instance: `d²v/(2(v+1)) + (v − log(1+v))/2`).

Mirror in the worklog theorem block after the Typing block (`:46-49`), and add a row to `docs/derivations/2026-08-12-exact-two-channel-finite-elbo/evidence/pifb2-crosswalk.md` beside line 12: *"PIFB2 observation display (:669/:689) | typing mismatch; not matched by this theorem."* Cite `roadmap:104` and `rm-03:365` as the standing internal registers — this is not a new discovery, it is an unpropagated one.

### E2 — `Theory/PIFB2.tex:689` and `:750` (Finding 1, part B)

Execute the step `panelA-T-SIMUL-derivation.md:333` already names. Bind `m_i`: write `−χ_i E_{q_i(c) ⊗ s_i(c)}[log p(o(c)|k_i,m_i)]`, which makes the closed theorem exact on that term and simultaneously repairs `:689`'s free symbol. Then reconcile `:669`, `:918`, `:941`, `:2679`, `:2822`, `:3211` to the same convention. Record in the commit that the joint typing is what the ELBO forces and that the predictive alternative has no exact ELBO reading.

### E3 — worklog §3c.3–3c.5 (`:240`, `:247-252`, `:290-293`, `:309-311`) and `overview.md:165-173` (Finding 2) — **blocking**

1. **Delete** `:240` (*"A base-neighbour block is precisely residual cross-design dependence"*).
2. **Replace** the Proposition at `:250`. Either it is tautological or it is false; the true statement is: *"At every fixed finite Λ_h the base-neighbour transported-KL sector is an exact negative-ELBO component of a normalized generative law that is a strict product over design points conditional on H_n. What it costs is not `hyp:gen-design-product` but the addition of an H_n-measurable transported label-copy channel (J_a, X_a) to the declared generative class of `Theory/04`, plus the replication multiplicity m_h."*
3. **Strike the iff at `:290-291` in both directions.** "⇐" is refuted by the witness (exact sector, hypothesis intact). "⇒" is unsupported: relaxing the hypothesis yields an interaction energy `−E log ψ(k_a,k_b)` (verified to 5.6e-17), not a transported KL — different functional, different value (0.3063 vs 0.2532 on a matched instance). State at most: *"a nonzero transported-KL sector requires a generative channel that reads beliefs at other contexts; it does not require, and is not implied by, stochastic cross-design dependence."*
4. **Replace `:292-293`:** `η_q` is fixed by the declared source row β and the declared replication `m_h = ⌈d h^{-2}⌉` (§4.1(ii); skeptic attack 10) — with `Σβ = 1` and weight `h^d` the coefficient of `½∫‖D^A q‖²` is forced to exactly 1. It is a declared coefficient, not a measure of dependence. Re-word roadmap experiment **E4** accordingly.
5. **Narrow the boxed `:309-311` / `overview.md:170-173`** to: *"C earns its manifold structure iff the action contains a term coupling distinct contexts; such a term is available from an exact finite ELBO with `hyp:gen-design-product` intact, via a lagged, history-measurable transported label-copy channel."* Drop "iff the generative model admits cross-context dependence." The idle-wheel half is untouched and should be kept.
6. **Flag the two inherited loci** that now carry the defective premise: `panelB-V-BRIDGE-derivation.md:67` (H5) and `:314` (OB-12), both self-labelled inherited.
7. Update `overview.md:198` open decision 4 — *"whether they agree in a dt→0 limit"* is no longer open; §4.5 answers it **no**. (Scope note: §4.5's counterexample is a *directed peer* 3-cycle, while the base-neighbour stencil is edge-symmetric, and `worklog:967` predicts agreement up to halved coupling under edge symmetry — so cite §4.5 for decision 4, not as a general lagged/contemporaneous inequivalence.)

### E4 — `overview.md:106` (Finding 3)

Change *"**EXACT** identity by KL disintegration"* to *"**PROVEN, CONDITIONAL** — exact given a normalized posterior, a recognition-independent measurable `C_h`, a common pushforward path, and a declared product reference measure."* Add a row: *"tied-replica/PIFB2 instantiation | **OPEN** — three explicit `C_h` supplied (§4.3), `ε_h` and `c_h` in closed form, but `ε_h = 0` only at `C_h = identity` and the law is blockwise-product, so it cannot pose the effective-action question."*

### E5 — worklog §4.3 (`:863-874`) (Finding 3, scope) — **the reviewer could not see this; it is the largest unreported defect**

§4.3 records the negative as *"which both agree on"* and restricts it to a balanced stencil, but omits the skeptic's counterweight entirely. Add: *"On biased rows the negative does not hold. Independently recomputed retained fractions of the peer sector at O(h²): 0.900 for a causal two-left-neighbour row `m = (−1,−2)`; 0.360 = (2w−1)² for an ALiBi-like `π = (0.8, 0.2)`; 1 for single-source agents. PIFB2's deployed rows (causal masking, ALiBi, learned position bias — `PIFB2.tex:709`) are of exactly this type, so `panelA-T-RESID-derivation.md:43,:237`'s headline 'the Fisher-covariant Dirichlet peer sector is not generated' is a balanced-stencil artifact and must be scope-restricted."* Also note `:197`'s cumulant series diverges for `D > π` (30-term partial sums: −0.4338 exact at D=2; −2.002 vs −1.087 at D=3.5; −5.3e4 vs −1.814 at D=5). Statement (4) is already marked false at `worklog:858-859` — no further edit needed there.

### E6 — worklog `:97` and `overview.md:162-164` (Finding 4)

`(C3)` is wrong **twice over**. (a) *"forced"* overreaches: `h^{d-2}` is forced as the unique deterministic Riemann-sum weight, but the exact ELBO is a counting-measure sum with unit coefficients (`boundary-counterexamples.md:63`) and supplies `h^{d-2}` only via the integer replication `m_h = ⌈d h^{-2}⌉` of `panelA-T-GRAD-derivation.md:41` (F) — a declared, canonically forced postulate. (b) The word *"therefore"* misattributes the forcing to the `h³` cancellation. `panelB-V-BRIDGE-derivation.md:63` carries an explicit **"NOTE (correction to the worklog's framing of E2/C3)"**: the symmetric-stencil cancellation is *not* needed for the limit to exist — an `O(ε³)` per-bond remainder contributes `O(ε)` and vanishes regardless; the cancellation improves the **rate** from `O(ε)` to `O(ε²)`. I confirmed this numerically: the oriented, uncancelled sum converges to the same target at `O(h)` in both the flat and covariant settings. The weight is forced by **counting alone**. `overview.md:164` is additionally stale on the other half — "covariant case open" is closed by §4.1(i); point at it.

### E7 — worklog `:1087-1089` (new defect in §4b; **no referee saw this**)

The hand-written synthesis asserts the connection mismatch *"explains the d=2 coincidence in 4.6 and the Gaussian-fixed-covariance escape in 4.1 as the same phenomenon: both are exactly where the Amari–Chentsov contraction drops out."* Contradicted by its own source: `panelB-V-BRIDGE-derivation.md:17` — *"The statistical nature of the target (curvature of g^F, the Amari–Chentsov tensor) plays **no** role in (V1)."* Direct numerical refutation: the `d=2` raw unit-coefficient bond sum converges cleanly with a **Poisson** fibre, for which `T_skew = A''' = e^θ > 0` everywhere. Soften to the hedged form the worklog itself used earlier at `:673-674` (*"plausibly one phenomenon and worth checking as such"*), and give the accurate unification: all the `d=2` statements are the classical scale-invariance of a two-derivative energy density in two dimensions, which `V-BRIDGE:197` already calls *"not an analogy; it is an identity."*

### E8 — worklog `:428`, `:479`, `:480-483`, `:544-545`, `:533`; `overview.md:151`, `:182`; and `u1_two_path_holonomy_witness.py` (Finding 5)

- `:428`: replace *"generates nontrivial holonomy **iff** A has curvature"* with *"…whenever A is not gauge-trivial. Nonzero curvature implies nontrivial restricted holonomy (Ambrose–Singer); the converse is false — a flat connection on a base with nontrivial π₁ has nontrivial monodromy and identically zero curvature."*
- `:479`: *"The residual is not noise — it is **holonomy**: the monodromy of A around γ′∘γ⁻¹. On a base of dimension ≥ 2 the infinitesimal version is curvature (cf. the plaquette expansion at `worklog:810-811`)."*
- `:480-483`: replace *"frustration becomes a bundle invariant"* with *"…an invariant of the flat connection modulo gauge, a point of Hom(π₁(C),G)/conj. It is not a bundle invariant: in the witness the bundle is trivial (H²(S¹;Z) = 0) and the holonomy varies continuously over the modulus space U(1)."*
- `:544-545`: **delete** *"for abelian U(1) the statistic *is* the holonomy (conjugacy class = element)."* Replace with the true statement plus a footnote at the `3π/2` row (`:533`): *the statistic is `arccos(cos Θ)`, the complete gauge-orbit invariant of this equal-weight two-path design; the `π/2` and `3π/2` record laws are gauge-equivalent (orbit distance 1.06e-16 at gauge angle 3π/4, exact for every Θ under `g* = π/2 + Θ/2` plus a label swap), so the `3π/2` row is the `π/2` row mirrored and is not independent evidence.*
- `overview.md:182`: *"equals the holonomy"* → *"separates the holonomy up to `Θ ↔ −Θ`."*
- `overview.md:151`: qualify **"Defeated"**: *"B4's **holonomy** clause defeated, for flat monodromy only. B4's curvature and bundle-topology clauses are untouched: `F` vanishes identically on any 1-dimensional base and every principal U(1) bundle over S¹ is trivial. Testing those requires C = T² (curvature, nontrivial H²) or S² (Chern classes, matching B4's own F4 counterexample at `wave2-01:674-680`)."* Note `worklog:466-468` already says the ledger entry *"reopens as genuinely open"* — the overview row is the one out of step.
- **Script:** the reviewer's prescribed fix ("add an oriented observable") *will not work alone* — with equal weights no observable of `p(o|Θ)` separates `+Θ` from `−Θ`, because the record laws are literally gauge-equivalent. The design must change first: set `PI_ROW = np.array([0.7, 0.3])` at `:78` (verified: orbit distance then 0.235 > 0), **then** replace `separating_statistic:149-154` with `np.arctan2(m1[0]*m2[1] − m1[1]*m2[0], m1 @ m2)` (gauge drift 8.9e-16). Comment that the prior asymmetry is a prerequisite, not cosmetic. Also fix the false docstrings at `:151`, `:217-218`, `:314-315`.

### E9 — worklog `:156` and `overview.md:153`, `:194-195` (Finding 6)

Replace PA-2 with the positive-definite no-go plus the complete classification: *"The Ad-invariant symmetric bilinear forms on gl(K,R) are exactly the 2-dim space {a·tr(XY) + b·trX·trY}. With `S = E₁₂+E₂₁`, `A = E₁₂−E₂₁` traceless and `tr(S²) = +2`, `tr(A²) = −2`, every form with a ≠ 0 is indefinite — signature (3,1) at K=2, (6,3) at K=3, with the trace direction flipping across `a + bK = 0` (the Killing form sits on that degenerate locus, signature (2,1,1)/(5,3,1)). The **only** semi-definite elements are the line a = 0, b > 0: `b·(trX)(trY)`, Ad-invariant and nonnegative but with radical sl(K,R) — Maxwell for the determinant line, vanishing identically on sl(K)-valued connections. The correct no-go is **positive-definiteness**, not indefiniteness of every element."*

**Verifier correction, governing:** the adjudication claimed only rm-04's §0(4) headline overstates. False. The overstatement recurs in **three** places: `rm-04:43-46` (§0(4)), `rm-04:295-299` (the §1.5 boxed either/or, *inside* the reviewer's own cited range `:275-302`), and `rm-04:879` (finding **K1** opens *"cannot be simultaneously Ad-invariant and nonnegative"*). Only the computation at `:285-288` and the script ledger at `:984`, both scoped to positive-definiteness, are correct as written. Do **not** point PA-2's source column at K1 as authoritative; record the erratum naming all three loci. Do not edit `rm-04` itself — it is an archived verbatim return.

For `overview.md:194-195`: compact type is necessary *and* sufficient for a **fixed** Ad-invariant positive-definite form, hence for a coercive fixed-inner-product curvature sector; it is **not** required for gauge invariance or nonnegativity as such — §4.2's Fisher-dressed form is exactly invariant for noncompact G, at the cost of being degenerate on the belief's isotropy algebra and noncoercive along gauge orbits.

### E10 — worklog §6 write policy `:1128-1131` and the gate at `:173` (Finding 7)

Name `Theory/` explicitly and add a **per-result SPEC:17 compliance check** alongside the existing rm-01…rm-06 reconciliation gate. Record that `SPEC.md:20-25` licenses (a) deriving a categorical source-label attention `β_ij` from a fixed normalized interaction-record joint and (b) holonomy-conditioned projection of transported marginal laws — so only the PIFB2-crosswalk results are actually blocked: **T-RESID's `ε_h`/`c_h`** (defined as a residual against `S_h^PIFB`, i.e. PIFB2 *is* the crosswalk) and **T-COEF's `τ = κ√K_q` obstruction**. Not blocked: §4.1(i) covariant parity, §4.1(iii) connection mismatch, §4.2 belief-dressed Ad-invariance, §4.6 d=2 bound.

### E11 — `docs/derivations/2026-08-12-pifb2-elbo-program-decision/final-report.md:14` (Finding 8)

Delete *"A separate revision-bound verification ledger…"*. Cite `PIFB2.tex:3280 (thm:state_level_elbo_nogo)` and `docs/derivations/2026-08-12-elbo-to-effective-section-action/claim-ledger.json` claim `live-peer-fixed-joint` (state REFUTED, `evidence_ids: ["ev-counterexamples"]`, artifact `evidence/adversarial-counterexamples.md`) with revisions. This is verbatim `rm-05:138-140`'s own prescription, outstanding since that report landed.

### E12 — minor edits

- **(i)** `overview.md:95, :105, :106, :136`: `finite-lattice` → `finite-site`. Propagate to the effective-section-action artifacts that originated the word: `construction-or-strongest-theorem.md:4`, `claim-ledger.json:139`, `release.json:9`. Reserve "lattice" for the `Λ_h` of §3c.2, which exists only once `hyp:gen-design-product` is relaxed.
- **(ii)** `worklog:484-487`: state that the ω-horizontal lift has `L^ω = 0` by `05c prop:pb-curve-taxonomy` (ESTABLISHED), so vertical Fisher length cannot weight the transport curve — `w(γ) ≡ 1`. If a weight is wanted, declare the **section-curve** length `L^ω(s∘γ) = ∫√(h_s^ω(γ̇,γ̇))` instead, and label ξ an explicit free parameter. **Delete "derived rather than fitted."** Note the worklog already carries the standing warning against this exact use at `:379-381`.
- **(iii)** Reorder §3d.7 before §3d.8 and rewrite the status line: *"WITNESSED (existence, abelian G): obligations 1–2 discharged; obligation 3 discharged for abelian G by CHECK 2; obligations 3 (non-abelian) and 4 open,"* matching `:681-683`.
- **(iv)** Rebuild the location tables at `overview.md:222-232` and `worklog:179-184` from `git ls-files` — the Desktop checkout tracks all four derivation runs, rm-01…rm-06, both ultradeep waves, and the 16 panel returns. Delete `overview.md:229` ("exist in only one copy"). Fix the stale absolute `Documents/ChatGPT` path at `worklog:143`, which contradicts `worklog:650-651` in the same file.

---

## 4. What the reviewer got right that the programme had not registered — including two anticipations §4b later confirmed

1. **Finding 6, part (c) — a clean blind hit.** The prediction that Fisher dressing would give *a nonnegative invariant state-dependent sector, stabilizer-degenerate, noncoercive along noncompact gauge orbits* matches T-CURV three for three: invariance without compactness or an Ad-invariant form (`panelA-T-CURV-derivation.md:23-25`, `:20`); radical = isotropy algebra `so(K−1)`/`so(K)` (`:31`, `:200`); coercivity fails (`:35`), sharpened by the skeptic to *"invariance and coercivity are structurally incompatible here… the ellipticity ratio is unbounded on **every single** gauge orbit"* (`skeptic:33`). The panels post-date `caa4a15` (`git ls-tree -r caa4a15` contains 0 files under `panels-2026-08-12`; `4dee0db` contains 17). Notably the T-CURV derivation agent *itself* used the reviewer's preferred wording independently at `:151` (*"an Ad-invariant **positive-definite** form on g (none exists on gl(K,R))"*), which is further evidence that `worklog:156` is the outlier.
2. **Finding 2's dichotomy, anticipated and then proven.** `panelA-T-GRAD-derivation.md:328` restated the reviewer's premise blind — *"the target's displayed formula drops the time superscripts and thereby **hides a mixed-time object**"* — and `:351` gave the dichotomy verbatim. The skeptic, while overturning the derivation's mass-term conclusion, explicitly endorsed the mixed-time reading (`skeptic:134`). Then §4.5 (T-SIMUL, the one Panel A target whose skeptic returns `survives:true`) proved same-time PIFB2 is **not** the `dt→0` limit of the lagged scheme, answering `overview.md:198`'s open decision 4 in the negative.
3. **Finding 5's curvature localization, confirmed from the other side.** §4b's plaquette expansion `D_KL(q‖(H_P)#q) = ½h⁴‖F_μν(c)·q‖²_{g^F} + O(h⁵)` (`worklog:810-811`) requires a 2-dimensional base — independently confirming that curvature lives exactly where the reviewer says it lives, and not where the witness sits. The ambient corpus already enforced the distinction the reviewer demanded: `panelA-ground-03-theory-corpus-sweep.md:262` reports that `11_obstructions.tex:422` and `12_philosophy.tex:111,326` already disclaim *"evidence of base curvature or bundle topology,"* and that no free energy in `Theory/` contains a curvature term. The worklog and overview are the only places that overreach.
4. **Finding 1, confirmed post-review by two independent agents.** `panelA-ground-01-pifb2-deployed-action.md:369` (gotcha #6): *"This is a real typing mismatch, not a notational one."* `panelA-T-SIMUL-derivation.md:251, :302, :333` leaves it unresolved and lists deciding it as a required next step: *"a one-line fix in the algebra but changes what −log p(o) actually is."* Also `worklog:925-926` newly records the T-COEF skeptic insisting *"the word **deployed** be dropped: S is the τ=1, unit-coefficient, χ-free skeleton, not PIFB2's actual functional"* — the reviewer's headline reached by a different route.
5. **Minor (ii) is stronger than the reviewer knew.** Not "underived": identically 1.
6. **Finding 8's redirection is correct** and the mathematics behind the no-go is sound — the defect is purely provenance, exactly as filed.

---

## 5. What the reviewer missed — defects a review of this programme should have caught

**Visible at `caa4a15`, and missed:**

1. **Finding 1 is not merely unproved — it is ill-posed, and the programme said so first.** `docs/research-plans/2026-08-12-pifb2-continuum-roadmap.md:104` already states: *"An expectation under a belief law on k cannot contain an unintegrated random model variable m,"* and `rm-03-action-class.md:363-365` calls it *"a live bug in the parent manuscript… should be carried into WP0 as a correction."* The reviewer cites neither, and files as new a defect the programme had registered twice — and left unfenced in the public overview. The *right* framing is: internally registered, publicly unfenced, and the culpability lies with PIFB2's display, not the theorem's algebra (the joint typing is ELBO-forced).
2. **Finding 2's *iff* fails in both directions, and the witness alone settles it.** The reviewer used equal-marginal dependent Bernoullis (refutes only a pointwise reading) and unequal-marginal independent Bernoullis (subsumed). The programme's own witness gives a strictly product law with a strictly positive base-neighbour sector — refuting "⇐" under every reading. And "⇒" is unsupported too: relaxing `hyp:gen-design-product` produces an interaction energy, not a transported KL. **Also missed:** the honest residue — the lagged witness is not in `Theory/04`'s declared class either, since it *adds* a label-copy latent block `(J_a, X_a)`. The sector does cost a modification of the generative model, just not the one named.
3. **Finding 4's `O(h)` claim is not unconditional.** The oriented Poisson sum on a reflection-symmetric section fits exponent 2.000, because the coefficient `−(1/12)∫A'''(θ)θ'³dc` is annihilated by parity (I measured `∫ = −1e-15`). Generically nonzero (−8.0455 analytic vs −8.0455 measured on an asymmetric section), but a referee reproducing it carelessly would "refute" the reviewer.
4. **"Only in d=2" invites an over-reading the reviewer did not block.** `d=2` does *not* rescue the construction: the bond/site relative weight mismatch is `h^{-2}` in **every** dimension (`panelA-T-GRAD-derivation.md:39` (E), stated d-independently), `V-BRIDGE:198-220` builds its own limit with two weights at `d=2` as well, and the site sector still grows as `h^{-d}` there (6.55e4 at M=256). `m_h` replication is still required at `d=2`.
5. **Finding 8's prior art.** `rm-05:69` files it at severity **HIGH (provenance)** and `:138-140` goes further than the review: it ran the grep proving no such ledger exists in any mounted repo, identified `verified-ledger.md` as a decoy (the GL(K) manuscript ledger, containing no entry for the state-level no-go), traced *"roadmap line 174 verified → final-report.md:14 → nothing resolvable,"* and specified the exact repair. Filing it again without attribution understates how long it has sat open.
6. **Minor (iv) recurs inside the worklog** — `:143` and `:179-184` — in self-contradiction with `:650-651`.
7. **`overview.md:151` stamps B4 "Defeated"** on a **three-clause** negative (holonomy, curvature, topology) when only the holonomy clause has a witness. `worklog:466-468` uses the careful phrasing; the ledger row does not.

**Only visible with §4b — flagged here because the PI has it:**

8. **`worklog:1087-1089` over-unifies `d=2` with the Amari–Chentsov drop-out** (E7 above), contradicted by its own source and by a Poisson-fibre `d=2` run. Note the same merger appears *hedged* at `:673-674`, so `:1088-1089` is an unhedged duplicate.
9. **§4.3's peer-sector negative is a balanced-stencil artifact** (E5 above). Biased rows — precisely the ones PIFB2 deploys — retain up to 90% of the sector at `O(h²)`. This is the most consequential unreported defect in §4b, because it partially reverses a headline negative.
10. **`worklog:97`'s `(C3)` is wrong twice**, and `panelB-V-BRIDGE-derivation.md:63` is the only place in the corpus that says so — an explicit *"NOTE (correction to the worklog's framing of E2/C3)"* that no downstream document has picked up.
11. **The T-GRAD panel's own conclusion line is negative** (`panelA-T-GRAD-derivation.md:53`: *"the gradient sector is **NOT** genuinely ELBO-derived"*, agent verdict `survives: false`), and `(F)`'s own text at `:41` calls `m_h` *"an extra declared h-dependent generative postulate, not a consequence of the theorem."* §4.1's positive half rests on evaluation **at the diagonal** under postulate (P2)'s unproven `lim_{h→0} lim_{n→∞}` exchange (`panelA-T-GRAD-derivation.md:358`; `worklog:789-791`: *"pointwise convergence at a fixed C² section, **not** Γ-convergence"*). None of this is visible from `overview.md`.
12. **`panelA-T-GRAD-skeptic.md:35-39` finds a hole in (H2)(b)** — it does not license the `|α| = 1,2` log-derivative differentiations Prop 2.2 performs — which is a live, cheap gap in exactly the uniformity apparatus a Riemann-sum theorem needs.
13. **§4.4 (T-COEF) is an open dispute** with derivation and skeptic reaching opposite verdicts; nothing from it is exportable. The review's claims table has no row for it.
14. **Section 3d.7's obligation-3 status** is a *scope* statement (abelian discharged, non-abelian open, `:568-570`), not a contradiction — the reviewer's "obligations 1–3 need reconciliation" is coarser than the file.

---

## 6. Revised continuation order

Reconciling the review's *Recommended continuation order* (`review:125-132`) with the worklog's own revised list (`worklog:660-689`). The two lists are largely **complementary, not competing**: the reviewer's items are claim-hygiene and typing repairs; the worklog's are mathematical unblockers. Only one item genuinely conflicts — the reviewer's #3 (*"instantiate `C_h` and compute T-RESID"*) is already **done**, and the worklog's #5 is its correct successor.

**Phase 0 — Claim-hygiene pass. Hours, not days. Blocking on everything below.**
Execute E1, E3(1–7), E4, E6, E7, E8 (text), E9, E11, E12 in one commit. Rationale: `overview.md` is the public claim surface, it is unchanged since `caa4a15`, and it carries eight overclaims of which four were already registered internally. No mathematics is at risk; the cost of leaving them is that any future external reader repeats this review. *(Absorbs reviewer items 1-partial, 5-partial; worklog item 6-partial.)*

**1. Decide the observation-term typing and edit `PIFB2.tex:689`/`:750`/`:669`.** (E2 — reviewer #1; the step `panelA-T-SIMUL-derivation.md:333` already names.) One-line algebra, unblocks the equality claim, and simultaneously repairs `:689`'s free symbol. Note the asymmetry the reviewer did not draw: the theorem needs `ζ = q⊗s` to kill `I(K;M)`, and it is precisely under that mean field that the Jensen gap to the predictive reading is strictly positive — the two repairs pull in opposite directions, so the joint typing must be adopted by fiat.

**2. Settle the T-COEF dispute (§4.4).** (worklog #1.) The cheapest decisive item on either list, and nothing from 4.4 is exportable until it is decided. It reduces to one modelling question: *may a generative model declare a block-multiplicity prior matched to the recognition multiplicity?* Promoted above the reviewer's items because it is a yes/no author decision with a large downstream fan-out.

**3. Rewrite §3c.3–3c.5 with explicit lagged/contemporaneous semantics.** (E3 — reviewer #2, now merged into Phase 0's text pass, with the *mathematical* residue here: decide what the base-neighbour sector actually costs. The answer is a transported label-copy channel added to `Theory/04`'s declared class plus `m_h`, not `hyp:gen-design-product`.) Then propagate to `panelB-V-BRIDGE-derivation.md:67` (H5) and `:314` (OB-12).

**4. Supply a microscopic law with genuine cross-agent coupling.** (worklog #5 — **supersedes** reviewer #3.) The reviewer asked for `C_h` and `ε_h`; §4.3 delivered three explicit `C_h`, the exact pushforward, and closed forms for `ε_h` and `c_h` (`c_h`'s first definition anywhere). The finding that survives is worse than the one the reviewer filed: `ε_h = 0` only at `C_h = identity`, and blockwise-product rigidity (`P_h^n = ⊗_a P_a^n` by construction) makes the law structurally incapable of generating multi-body operators. Minimal repair: a shared latent, or a non-blockwise block-spin `C_h` whose coarse coordinates each read several agents' fine blocks. **Fold in E5** — the biased-row scope restriction — before anyone builds on §4.3's negative.

**5. Chase the connection mismatch (§4.1(iii)).** (worklog #2.) The one genuinely new obstruction: the ELBO's KL orientation forces Amari `α=+1` while the Fisher-Dirichlet action's EL operator is Levi-Civita, with gap `½(g^F)^{-1}T_AC(D_μq, D_μq)` that does not shrink with `h`. **Decouple it from the `d=2` claim** per E7 — the worklog's own linkage is unsupported, so do not let it steer the search.

**6. Lattice counting, mesh coefficient, boundary treatment, global covariant limit.** (reviewer #4, restated.) Three of the four sub-items are now supplied: multiplicity and the symmetric stencil at `panelA-T-GRAD-derivation.md:23-33`, boundaries at `:59` (an `O(h)` counting estimate, asserted in a hypothesis block, not proved), the covariant case closed at §4.1(i). What remains is the **variational** limit: equicoercivity, liminf, recovery sequences, interpolation topology, gauge compactness. `rm-06-prior-art.md:277` prices this as *"real new work — T8 is the most expensive item in the roadmap"* (Garcia Trillos–Slepčev covers only scalar/`R^n`-valued fields). **Do not schedule this before items 1–5.** Cheap sub-tasks to do now: the `(C3)` wording fix (E6), and extending `kl_expansion_check.py` with a refining-torus section that prints fitted exponents for oriented and symmetric sums under an explicitly stated edge convention plus the free-jet symbolic parity check — which would close the standing gap between what that script proves and what its `:100-102` claims.

**7. Write up the `d=2` induced-volume bound (§4.6).** (worklog #3.) The strongest base-geometry result the programme has, numerically confirmed to 1.7e-16, currently in no manuscript. Carry the three caveats (ρ exogenous; attainment fails on the rank-drop locus — `inf`, not `min`; every one-parameter fibre excluded by `dim B ≥ d`) and the corrected framing: the `d=2` selection is a homogeneity/Weyl fact about `h` as an abstract psd tensor, **not** an Amari–Chentsov fact. *(Note: `dim B ≥ d` is a prerequisite for `S_vol` to be non-identically-zero — it is **not** a prerequisite for the bond-sum limit, which holds for any `dim B`. Do not conflate them.)*

**8. Holonomy: discharge §3d.7 obligations 3–4, and strengthen the witness.** (reviewer #5 + worklog #4.) The language fix is Phase 0. The genuine new work is: (a) break the design symmetry (`PI_ROW = (0.7, 0.3)`) **before** adding a signed statistic — an oriented observable alone cannot work, because the equal-weight record laws are gauge-equivalent for every Θ; (b) a conjugacy-class invariant for non-abelian G; (c) whether curve-mediated coupling relaxes `hyp:gen-design-product` identically to §3c or strictly further. A `T²` or `S²` variant, to test B4's curvature and topology clauses, is new work — not a repair — and should be scoped separately.

**9. Governance and consolidation.** (reviewer #6 + worklog #6.) Add the per-result SPEC:17 gate to worklog §6 (E10) — the reviewer's blanket "keep it outside `Theory/`" is too broad, since `SPEC.md:20-25` licenses the two constructions most likely to be exported. Then consolidate the four repositories (now five artifact sets) before further theory expansion. Both are cheap and both are currently unowned.

**Summary of the reconciliation:** the reviewer's #1, #2 and #5 become Phase 0 + items 1, 3 and 8; his #3 is superseded by worklog #5 (item 4) because the work he asked for is done and the result is worse than he knew; his #4 becomes item 6 and is correctly the most expensive item, so it is scheduled last among the mathematical items; his #6 becomes item 9 in narrowed form. The worklog's #1 is promoted to item 2 on cost-to-decide grounds, and its #2, #3, #4, #6 land at items 5, 7, 8, 9. The single addition neither list contains is **E5 / E7** — two defects inside §4b itself that no referee has yet seen.